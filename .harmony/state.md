# Harmony State — Audio-DNA (RealTimeAudio)

Norm ID: 2
Last normalized: 2026-09-05
last_normalized_sha: d93e6ba
Normalized by: Harmony
Method: FULL Phase 2.5 deep-audit re-trace across HEAVY drift (127 commits / ~116 feature-affecting files since 9139dd4). 7 read-only lanes + completeness critic + integration writer + independent verifier. 6 CRITICAL and 18 MAJOR/MINOR corrections folded into FEATURES.md; every count re-derived from source, none carried forward. Validator: PASS=265 FAIL=0 WARN=1 COMPLIANT (--framework game). Lane reports: .audit/renorm-2026-09-05/.

## Norm records
- 2026-09-05 — **full Phase 2.5 re-trace** (norm v2, sha `d93e6ba`, HEAVY drift: 127 commits / ~116 feature-affecting files since `9139dd4`). Method: 7 parallel read-only lanes (audio-analysis, render-effects, sources-media, control-plane, ui-surfaces, io-api, docs-tests-build) → completeness critic → single integration writer fenced to FEATURES.md → independent verifier who re-ran the validator and re-derived 4 high-stakes numbers from source. **6 CRITICAL corrections**, the largest found independently by three lanes: the doc's banner asserted "Undo/redo remains a no-op (Wave 2)" while a full Undo v1 had shipped (8 new src/core files, 17 Command subclasses, Cmd/Ctrl+Z wired end to end). Also: FeatureBus documented as a triple-buffer swap when `cb4d5fa` had replaced it with a seqlock/Writer-handle protocol; a documented `MappingEngine::processAll` that does not exist (it is `processFrame`, moved off the render callback onto a 120Hz message-thread timer by `c51aff7`); and a §14 DATA RACE gotcha describing a race fixed on Jul 30 by `f6b208f`/`8077af7`. Counts re-derived: embedded shaders 244→243, test_effects.py discovery 112→135, REST endpoints 22/21→24 registered / 23 production, `u_beatPhase` real uses 11, `uploadAudioUniforms` 28→29. Dead surfaces confirmed by tracing to a consumer (2 grep patterns each): `bpmMultiplier`, `quantizeMode`, `smartAutopilotEnabled` (unreachable by any user path — its only setter has no callers), and the BPMSync/ClipPosition/Timeline param sources. Findings the critic flagged as unverified were parked in an UNRESOLVED list and deliberately NOT folded in; the verifier confirmed none leaked into the doc. Build PASS, ctest 203/203. Also this session: VALIDATION.md created (was the one missing REQUIRED template file), binding-decisions.md created, FEATURES.md §24 gap closed, gotchas.md schema repaired to a form the promotion scan can actually read.
- 2026-07-16 — full re-audit re-norm (norm v2, zero code drift, HEAD 9139dd4): 7-lane deep-audit swarm re-derived all counts from source; APP-INVENTORY.md created; FEATURES/FEATURE_MAP/file_map corrected; graphify: pre-session working-tree drift (partial 2,416-node regen) REVERTED to the committed 4,231-node/13,665-edge graph (d4d2c83, 2026-05-19); no regen this re-norm (zero code delta). Build PASS, ctest 113/113. Verified counts: 135 effects/11 cats/333 params, 15 transitions (+1 deck), 244 embedded shaders, 108 sources/18 cats/759 params, 30 audio features/14-stage pipeline, 58 mapping sources/24 curves, 22 REST endpoints (21 functional), 11 OSC patterns (inert).
- 2026-05-24 — delta re-normalize (norm v2, SHA 4ee10ad): 8 docs/design commits, no code changes; FEATURES design-direction notes, CONTEXT +5 terms, FEATURE_MAP date refresh, graphify unchanged.
