# s-rta-0924 work log (secondary)
- boot: handoff s-rta-0923 read; screen-safety law read. Tree: .harmony-version modified (not ours), AGENTS.md untracked (not ours) — leave.
- #1 comp-position units bug: builder dispatched (TDD, scratch dir fail-first). Gate = probe-lane3.sh COMP check (expect 12/12 after).
- #2 step 3: architect critic dispatched -> .harmony/.reports/s-rta-0924/step3-critic.md (S3-M superseded by Lane 3 ManualWrite seams).
- rig note: MINIMAL-profile read-only REPORT_FILE must be under <repo>/.harmony/.reports/ (dispatch gate blocks memory/.reports/ in a foreign repo).
- Boris 2026-09-24: Bluetooth headset crash re-test DEFERRED ('another time, not that important') — stays on loose-ends as low priority, not owed this session.
- #1 builder DONE: ScalarMath::posPxToCompUniformX/Y (px/1920, px/1080), Renderer applyCompTransform uses them for position+anchor; new test_comp_transform_units (5 cases). ctest 362/362 (builder-reported).
- #1 HARMONY LIVE GATE: probe-lane3.sh 12 PASS / 0 FAIL (was 11/1). COMP frames no longer black: comp_a mean 77 / black band right 8.1% cols; comp_b black band left 2.9% -> real horizontal shift. Looked at comp_a.png. App gone, 0 Output windows. Reviewer pending -> then commit.
- #2 critic DONE: BUILDABLE-WITH-FIXES, 5 blocking, A1-A8 adopted; banner added to plan. Report .harmony/.reports/s-rta-0924/step3-critic.md.
- step3 orchestration: W1 workflow = lanes S3-A (recording), S3-C (REST), S3-G (gate assets) in worktrees, each builder->reviewer->1 fix round. Harmony merges, builds, ctest. W2 = S3-B (MainComponent) + S3-D docs. Then Harmony runs probe-step3.sh.
- W1 workflow launched: run wf_4ad45345-ed0 (task wnyofy2y4). Script under ~/.claude/projects/.../workflows/scripts/rta-step3-wave1-*.js. On completion: merge lane branches to main in order C, A, G; one clean build; ctest; then W2 (S3-B + S3-D).
- W1 DONE: S3-C a2c5b50, S3-A c737d3d, S3-G 57ffe74+ae2eb17 (1 fix round) all reviewer-APPROVE; merged bacc6c3/615473c/fa67f0a; build rc0, ctest 377/377 (Harmony-run). Pushed. W2 = S3-B MainComponent next; then Harmony live probe-step3.sh; then S3-D docs.
- W2 launched: run wf_3879ebde-fa2 (task w3ufsvhn2) — S3-B builder + 2-lens review (spec, threads) + <=2 fix rounds. Then merge, build, ctest, Harmony runs .harmony/probe-step3.sh live, then S3-D docs.
- Boris 2026-09-24: 'keep working till you hit 50% ctx then eos' — off-ramp = 50% [CTX] gauge; then eos-secondary. Parallel while W2 runs: R13 (non-48k device) plan by architect (read-only).
- W2 DONE: S3-B b5931e8, both reviewers APPROVE r1; merged f6198da; build rc0; ctest 377/377 (Harmony). Deviation to rule on: LayerTransport play now resets clip->reverse (applyClipPlaying consolidation).
- LIVE GATE probe-step3.sh run 1: FAIL on probe string-compare "48000.0"!="48000" (probe defect) -> Harmony added normnum() to .harmony/probe-step3.sh.
- LIVE GATE run 2: 51 PASS / 5 FAIL. PASS: arm, provisional take, 4 triggers, set_bpm, periodic save, stop, store asset+sidecar, take v3, seg rate 48000, fp1, tempo 12800, load Resolved, wallClock replay reaches 3, overdub safety (1 point, same asset, 1 asset), screen law. FAIL analysis (take ~/Documents/Audio-DNA/Takes/step3gate1.adna-take):
  (a) opacity 3 gestures x 2 breakpoints: CORRECT product behaviour (3 REST writes 1 s apart, decaying hold 250 ms each) -> probe expectation wrong.
  (b) replay order_ok=0: first sample (col 2) is leftover pre-replay state; recipe ends with trigger_column 2 so true seq is 0,1,2,3,2 -> probe expectation wrong, BUT checkpoint0 (col -1, opacity 1.0) apparently NOT restored at replay start -> suspect.
  (c) replay opacity never >0.2 -> continuous replay not landing? suspect real.
  (d) markers=0 -> T2 alignment NA; onset markers not recorded? suspect.
  (e) checkpoint0.bpm=0.0 at arm (no tempo yet) -> note.
  -> diagnosis agent dispatched (live app, no src edits).
- RULING (Harmony): LayerTransport play must NOT reset clip->reverse (a pad pause/play on a reversed clip flipping it forward is a live-performance regression). TopBar Play keeps resetting reverse. Fold into the post-diagnosis fix round (applyClipPlaying gets a 'resume' variant that leaves reverse alone).
- R13 plan DONE (.harmony/.reports/s-rta-0924/r13-plan.md): resample to 48k on analysis thread, bypass at 48k, band gating + bandValidMask, rateMismatch retires -> rateChangedSinceArm. Now: workflow critic -> lane B -> lane A (same worktree). Lanes C (RecorderHost) + D (MainComponent/probe) WAIT for step-3 fix-round merge.
- R13 W1 workflow launched: run wf_ba79ad19-875 (task w6gvqv2ou): critic -> lane B -> lane A (same worktree), each reviewed + fix rounds. Boris product calls from R13 plan (§9): Air band shows 'n/a' at 16k? ; file-mode analysis capped by a low-rate OUTPUT device?
- DIAGNOSIS DONE (step-3 gate FAILs): all 5 = probe/fixture defects, product wiring correct (live-instrumented: continuous replay 0.4->0.7->0.2 lands; refusedByHand=0). Harmony disk-verified: probe replay loop breaks on first col==3 (lines 396/416) before opacity gestures fire; gen-click-wav.py = 1-sample impulse (aubio specflux never fires -> markers 0). checkpoint0 is NOT applied on replay BY SPEC (s167 D4/D14: preamble = routines scope, deferred) -> Boris product question: should whole-take replay start from the arm-time state?
- NEW RESIDUE: diag run saw "[Recorder] audio tap self-stopped mid-take" with no device change (RecorderHost.cpp ~368-374). Not in Harmony's gate run. -> chase (would kill a show's audio).
- Next workflow: probe fixes + click burst | LayerTransport reverse fix (+review) | self-stop diagnosis.
- fixup workflow launched: run wf_57394d07-c3f (task wjoxwpc19): probe-fix (commits on main, 2 .harmony files) | reverse-fix (worktree)+review | selfstop diagnosis (live app). R13 W1 (wf_ba79ad19-875) still running. After both: merge, build, ctest, Harmony reruns probe-step3.sh.
- INCIDENT (my packet's fault): self-stop diag agent attached lldb -> Touch ID dialog -> sent Escape that hit another Harmony terminal session. Rule filed .harmony/gotchas.md (no debugger attach, no GUI input outside app, stop on unexpected dialog). Tell Boris.
- SELF-STOP = REAL race (4/4 repro): RecorderHost::arm sets tapWasRunningLastTick_=true optimistically (:210-211) before the audio thread's first push flips AudioTap::running_; a 120 Hz tick in between reports "self-stopped" + notify, and the edge latch then SWALLOWS a genuine later self-stop. Audio itself fine (gaps 0). Fix: latch tapHasEverRun_; re-arm notify when running returns. Harmony's gate did not hit it (race).
- fixup: probe fix 2b0168c on main (static-verified only); reverse-fix 0f4ff08 (worktree wf_57394d07-c3f-2) reviewer APPROVE; mirror-test only (T13-class gap, repo precedent).
- Gate run 3 (main bda999c, ctest 382/382): 53 PASS / 4 FAIL. Replay rows now PASS (probe loop fix works). RECEIVER-VERIFY: probe-fix agent CLAIMED the opacity rows were fixed; lines 293/295 unchanged -> Harmony fixed (3 gestures, values 0.4 0.7 0.2, 2 breakpoints; tested against the real take). Asset row counted earlier runs' assets (store never deletes) -> now counts the delta since arm.
- markers still 0 WITH the 5 ms burst -> prior "1-sample impulse" root cause DISPROVEN (it was inferred, never tested). Onset diagnosis re-dispatched (a8fab...): detector never fires vs one-hop pulse missed by 120 Hz tick vs other. Packet carries the no-debugger/no-GUI-input rule.
- self-stop fix DONE 33295d2 (worktree agent-a509d70dc7cc1ce94): seed tapWasRunningLastTick_=false; 3 tests [selfstop], fail-first RED on pre-push case; builder DISPROVED diagnosis claim 2 (re-arm already worked — test passed pre-fix). ctest 385/385 (builder). Reviewer dispatched (asks: never-started tap silent?).
- self-stop fix reviewer APPROVE (re-ran fail-first); merged. DEBT: tap armed but never started -> now silent (framesWritten stays 0, visible in status only) — needs a start-timeout / stall detector (loose end).
- ONSET DIAGNOSIS (definitive, offline replica harness of OnsetDetector/AnalysisThread + libaubio): aubio confirms a flux peak one hop LATE; for the click WAV that hop is true digital silence (-inf dB) -> its internal silence gate drops the onset regardless of threshold/silence/method (swept). Real audio fine (46 onsets on a real drum loop). Fix = fixture: gen-click-wav.py --floor-dbfs -50 default. Harmony re-ran the agent's harness: floor -50 -> 10/10 onsets on the grid; floor -300 -> 0 (control).
- LATENT (log, low sev): MainComponent tickFeaturePipeline reads FeatureBus::read() always-latest; a one-hop onsetDetected pulse can be lost if 2 hops publish between 120 Hz ticks (jitter) -> occasional missing onset markers / uniform pulses. Harden with a sticky onset counter later.
- LATENT (product note): percussive material with TRUE digital-silence gaps (gated/sparse electronic) would lose onsets the same way — rare with real audio.
- Gate run 4 (main 2ce3389, all fixes): 57 PASS / 1 FAIL — only T2 alignment (156 markers vs 108 peaks, drift 493 ms, p95 jitter 475 ms). Suspect duplicate markers (120 Hz tick re-reads same 94 Hz snapshot) + tick-time stamping. Offline diagnosis dispatched (a8b7...). self-stop count in stderr: 0.
- T2 DIAGNOSIS (offline): timing CORRECT once computed right — dedup+grid pairing: n=134, mean 31.96 ms (spec-expected lag), drift 0.13 ms (≤1), p95 jitter 8.04 ms (≤15); asset byte-identical to source click. PROBE defects: peak detector misses 18% (burst barely > threshold), nearest-peak pairing unbounded, drift = raw endpoint diff, jitter = |offset| not |offset-mean|. PRODUCT defect: duplicate onset markers ~14% (RecorderHost.cpp ~402: no dedupe on snap.timestamp). -> fix workflow.
- onset-dedupe workflow launched: run wf_a180f637-e8f (task wuid97b7d). R13 W1 (wf_ba79ad19-875) still running.
- R13 W1 DONE: critic BUILDABLE-WITH-FIXES (amendments in wf journal / scratchpad r13-w1-result.json); lane B c69d23a APPROVE-WITH-NITS, lane A 7db6dc6 APPROVE (reviewer forced bypass -> RED). Merged, build rc0, ctest 399/399 (Harmony). Resampler DORMANT until lane D (MainComponent ctor). Remaining R13: lane C (RecorderHost rateMismatch -> rateChangedSinceArm), lane D (MainComponent + probe + docs + live gate), optional E (UI).
- G5 relay (inbox routed item from primary s230) DONE: (+) PASS; (-) inconclusive (architect self-refused, hook not reached); new finding: reviewers bypassed the MINIMAL write BLOCK via Bash for fail-first reverts. Reported up via coord_pending_write. Stray memory/ dir removed.
- onset dedupe 7fe16b5 reviewer APPROVE (reviewer self-reported Bash edit of src in worktree — same fence-bypass already reported up); merged; build rc0; ctest 402/402 (Harmony).
- STEP 3 LIVE GATE (main, all fixes, Harmony-run): 61 PASS / 0 FAIL. T2: mean 30.69 ms, drift -0.94 ms, p95 jitter 5.35 ms, dupes 0, matched 94%. self-stop lines 0. R13 A-1 app check PASS: stderr "[Analysis] source rate 0 Hz -> 48 kHz path (no resampling), bandwidth 24000 Hz"; profile Resample 1 us.
- FLAKE RISK (open): T2 drift bound ±1 ms on a ~65 s take is at noise level (per-marker jitter ~5 ms, n~126 -> slope-derived drift stderr ~1.5 ms). Spec D10.3 measures minute 10 vs minute 0. Fix: longer take for the drift row (or bound = 1 ms + 2·stderr, report stderr). Not yet done.
- gate-fix agent gotcha (for notebook): heredoc inside $(...) with an odd number of apostrophes in its body breaks bash -n (bash 3.2) — reword comments.
- R13 W2 workflow launched: run wf_9b13b1a6-68f (task wwhdyyq7x): lane C (rateChangedSinceArm) -> lane D (MainComponent makes resampler LIVE, probe R13 oracle + T2 drift stderr flake fix, CLAUDE.md/APP-INVENTORY docs). Reviewers now told explicitly: no Bash writes. After: merge, build, ctest, Harmony live gate (probe-step3 + /api/features sourceSampleRate/bandValidMask), then EOS (off-ramp 50%).
- R13 W2 DONE: lane C 008dbc1 (rateChangedSinceArm; deprecated analysisRate/rateMismatch shims kept) APPROVE; lane D 1e0f893 (resampler LIVE via MainComponent, probe R13 provenance oracle, T2 drift stderr bound + WARN, CLAUDE.md/APP-INVENTORY) APPROVE. Merged; build rc0; ctest 404/404 (Harmony).
- LIVE GATE run 6 (resampler live): 62 PASS / 1 FAIL. R13 rows ALL PASS (sourceSampleRate 48000 == deviceRate, bandValidMask 127, warning retired, rateChangedSinceArm false; stderr 0 Hz -> 48000 Hz path lines). T2: mean 30.87, drift -0.10 (stderr 2.14 -> WARN, take too short), p95 5.54. FAIL: onset coverage 87.2% (<90%).
- ONSET LOSS = PROVEN PRODUCT DEFECT: offline replica detects 241/240 clicks on the same file; app recorded 115/133, misses scattered (not a stall). One-hop onsetDetected pulse lost between always-latest FeatureBus reads. Fix = monotonic onsetCount in FeatureSnapshot + count-delta in RecorderHost. LIKELY SAME LOSS for render-thread consumers (u_onsetDetected at 60 fps vs 94 Hz analysis) -> onset-reactive visuals miss beats. Builder dispatch BLOCKED by Harmony_Main frozen battery lock (pid 58189) — waiting.
- CLEANUP owed: RecorderHost deprecated shims (ArmOptions::analysisRate, Status::rateMismatch mirror) now removable.
