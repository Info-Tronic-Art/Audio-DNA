# Reviewer Verdict — rta-rev-L3S3-2026-09-05
STATUS: DONE
VERDICT: PASS
FILES: src/MainComponent.cpp, src/MainComponent.h (fence-clean; .harmony/gotchas.md and graphify-out/* are tooling's, ignored per instructions)
ISSUES: none blocking; 2 non-blocking observations (undo-clear is stricter than strictly required for append; repeated-click media re-open cost) — see body
METADATA: reviewer=rev-L3S3, builder_packet=L3-composition-persistence.md STEP3, date=2026-09-05T00:00:00Z

## Body

VERDICT: PASS

FILE: src/MainComponent.h
  [OK] Patterns: new decls (`openMediaForDeck`, `appendDeckFromFile`) placed next to Step 2's `swapCompositionModel`/`refreshUiAfterModelSwap`, matching existing grouping.
  [OK] Spec fidelity: header comment says `appendDeckFromFile` is NOT named `loadDeck` because that name is the legacy PresetManager path — verified `void loadDeck();` still declared at MainComponent.h:133 (legacy, untouched).

FILE: src/MainComponent.cpp
  [OK] Refactor identity (Adjudicate #1): diffed Step 3's `openMediaForDeck` body (MainComponent.cpp:2493-2523) against Step 2's inline loop as committed in `c261258` (MainComponent.cpp:2526-2561 at that commit) with the outer `for (auto& deck : incoming.decks) { … }` wrapper stripped — remainder is byte-identical (same conditionals, same order, same magic numbers 90/72, same comment). Only behavioural delta: `previewPanel_.getRenderer()` is now fetched once per deck instead of once per load; `PreviewPanel::getRenderer()` (PreviewPanel.h:35) is `{ return renderer_; }` — a plain reference getter, no side effects — so this is not an observable change. `loadComposition`'s new call site is a direct `for (auto& deck : incoming.decks) openMediaForDeck(deck);` replacing the block verbatim.
  [OK] Append semantics / fence (Adjudicate #2): `appendDeckFromFile`'s mutation lambda `[this, &incoming]{ composition_.activeDeckIndex = composition_.appendDeck(std::move(incoming)); }` runs entirely inside `swapCompositionModel` → `undoService_.withDeckDetached(mutation)` (MainComponent.cpp:2447; UndoService.cpp:32-89). Traced `withDeckDetached`: `activeDeck_` is nulled and one in-flight GL frame is drained (`executeOnGLThread(..., true)`) BEFORE `mutation()` runs, so `Composition::appendDeck`'s `decks.push_back` (Composition.h:164, which reallocates) executes with the GL thread parked. `ActiveDeckRestoreGuard`'s destructor re-resolves via `composition_->getActiveDeck()` (index-based, Composition.h:126) after mutation — picks up the just-set `activeDeckIndex` correctly regardless of whether push_back reallocated. Mutation lambda calls nothing that itself fences (no nested `withDeckDetached`) — respects the `jassert(!fenceActive_)` reentrancy guard (UndoService.cpp:41).
  [OK] ID safety (Adjudicate #3): order in `appendDeckFromFile` is STAGE → shape-check → `fromVar` → `validateDeck` (refuse/repair) → `compload::remintClipIds(incoming, s_nextClipId)` → `openMediaForDeck(incoming)` — re-mint strictly precedes any media open, same as `loadComposition`. `Composition::appendDeck` independently re-mints the deck's own `id` from `nextDeckId_` (Composition.h:164, landed in Step 1) — no collision path.
  [OK] Refusal posture (Adjudicate #4): independently re-derived the key-shape claims rather than accepting the builder's report — `Composition::toVar()` top level uses `"decks"` (Composition.h:240), never `"layers"`; `PresetManager::savePreset`'s FX-preset top level uses `"name"/"version"/"effects"/"mappings"` (PresetManager.cpp:76-104); the *legacy* `PresetManager::saveDeck` `DeckState` top level uses `"type"/"audioFile"/"slots"/"fx"` (PresetManager.cpp:493-495) — none of the three has a top-level `"layers"` key, so all three are refused by `appendDeckFromFile`'s `!obj->hasProperty("layers")` check before `Deck::fromVar` ever runs. Truncated/non-JSON: `juce::JSON::parse(String)` never throws — internally catches `JSONParser::ErrorException` and returns `var()` on failure (juce_JSON.cpp:541-548, verified in the vendored source at `build/_deps/juce-src/...`), so `parsed.getDynamicObject()` is `nullptr` → clean refusal, not a crash. Well-formed deck with zero layers: `compload::validateDeck` (Step 1, unchanged) returns a non-empty reason on `d.layers.empty()`, and the refusal happens after `fromVar` but strictly before RE-MINT/OPEN/SWAP — all four refusal paths leave live composition, media maps, undo stack and inspectors untouched (early `return` before any mutating call).
  [OK] Dangling-pointer class (Adjudicate #5): the whole fenced sequence (fence entry → mutation → `refreshUiAfterModelSwap`) is synchronous with no message-loop pump (confirmed by reading `withDeckDetached` and `refreshUiAfterModelSwap`, both unchanged by this diff), so no repaint/timer can observe a mid-mutation state regardless of whether the mutation is a `move`-assign (Step 2) or a `push_back` reallocation (Step 3) — the invariant this depends on ("no pump between fence entry and `rebuildGrid()`") is mutation-shape-agnostic. `rebuildGrid()` unconditionally destroys and recreates every `ClipCell`/`LayerStrip` (DeckView.cpp:66-176) off the post-mutation model, so it does not matter whether push_back moved existing `Deck` objects to new addresses.
  [OK] Undo (Adjudicate #6): `swapCompositionModel` calls `undoManager_.clear()` unconditionally on every path including append. This is CORRECT (no stale command can misresolve) but is stricter than strictly necessary for append specifically: unlike a full swap, an append does not renumber or invalidate any *existing* deck's coordinates, so commands referencing the surviving decks would in fact still resolve correctly. The architect's packet explicitly settled this as "one rule: clear on every load" (§2, RECON OPEN QUESTION 3) for uniformity/auditability rather than per-mutation-shape logic — a defensible, documented tradeoff, not a bug. Net effect: appending a saved deck silently discards the performer's undo history for the decks already on screen. Worth a plain-language confirmation from Boris that this is the desired live behavior; not a blocking finding since it is implemented exactly as designed.
  [OK] Deck tabs + active index (Adjudicate #7): `rebuildGrid()` calls `setupDeckTabs()` (DeckView.cpp:182), which iterates `composition_->decks` and adds one tab per deck (DeckView.cpp:402-424) — confirmed by direct read, not just the builder's citation. Active-tab highlighting is NOT done in `setupDeckTabs()`/`rebuildGrid()` itself; it happens in `DeckView::refresh()` (DeckView.cpp:239-246), which `refreshUiAfterModelSwap()` reaches via `deckView_->setActiveColumn(-1)` (unconditional `refresh()` call, DeckView.cpp:251-255) run AFTER `rebuildGrid()`. Since `composition_.activeDeckIndex` is set inside the fenced mutation before any of this UI code runs, the newly appended deck's tab is correctly highlighted by the time `refreshUiAfterModelSwap()` returns.
  [OK] Repeated clicks (Adjudicate #8): each click is an independent synchronous STAGE→VALIDATE→RE-MINT→OPEN→SWAP; `s_nextClipId` is a monotonic file-static, so 5 clicks never collide ids across the 5 appended copies. Tabs/active-index scale linearly with no observed overflow logic bug. One real (non-blocking) cost: unlike "Add Deck" (which creates an empty deck via `Composition::addDeck`, Composition.h:141 — no media at all), appending a deck WITH real clips re-opens that deck's underlying media files fresh on every click (a new `VideoPlayer`/decoder per click, same file). This mirrors dragging the same file into multiple cells elsewhere in the app, so it is not a new bug class, but it means 5 rapid clicks on a heavy deck spin up 5 independent decoders for the same source — worth the human resource-ceiling check called out below.
  [OK] Fence (Adjudicate #9): `git diff --name-only` for this session shows only `src/MainComponent.cpp`/`src/MainComponent.h` plus `.harmony/gotchas.md` and `graphify-out/*`, which are tooling/Harmony artifacts per the packet, not builder output — clean per §6's Step 3 allow-list.
  [OK] Scope (Adjudicate #10): no touches to `Renderer.*`, `CompositorEngine.*`, undo/command files, `TopBar.*`, `ApiServer`, or any §7 out-of-scope item. `kCompNew` unchanged (already routed through `swapCompositionModel` by Step 2). No dirty-flag/autosave/version-field/FilenameFilter creep.

SUMMARY: 2 files, 0 blocking issues, 2 non-blocking observations (undo-clear scope for append; repeated-click decoder cost). All 10 adjudication points independently re-derived against source (JUCE `JSON::parse` source read directly to confirm no-throw; Composition/PresetManager/legacy-DeckState top-level keys re-grepped rather than trusting the builder's claim; refactor diffed line-by-line against the Step 2 commit rather than eyeballed).

## What only a human at the machine can verify
- Click a saved deck row with ≥1 real video clip and ≥1 image-sequence clip: new tab appears and is highlighted active, its clips trigger and play (proves OPEN NEW + re-minted ids end to end — nothing machine-side can drive `MainComponent`).
- Switch back to the deck that was active before the append: its clips still play (confirms no dangling-pointer regression from the push_back reallocation specifically, as opposed to Step 2's move-assign case already exercised).
- Click the same deck row 5 times fast: 5 new tabs, no crash, `/api/composition` shows all clip ids unique and ≥1000; watch memory/CPU for the decoder-duplication cost noted above under a deck with several video clips.
- Drive each of the four refusal cases through the actual Decks browser row (not just `loadComposition`'s Open dialog, which Step 2 already covers): a composition `.json`, an FX preset `.json`, a hand-truncated `.json`, and a well-formed deck file with `"layers": []` — one alert each, no tab added, no undo-stack change, previously active deck keeps playing.
- Confirm with Boris that clearing all undo history on a deck-append (not just on New/Open) is the intended live-performance behavior, since it also discards undo for edits made to decks that were NOT touched by the append.
- Save the composition after an append (Cmd+S), then reopen it fresh: the appended deck's name, layers and clip references round-trip correctly through `Deck::toVar`/`fromVar`.
