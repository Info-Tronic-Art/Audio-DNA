# Reviewer verdict — Lane L6 (EnvelopeSignal default-mode siblings, addendum 5)

VERDICT: APPROVE

## Findings

1. NON-BLOCKING (informational, verified clean): File fence respected. `git diff --stat`
   in the worktree shows only `tests/test_oscillator_bar_fold.cpp` modified;
   `build-lane/` is untracked scratch as claimed. `src/signal/EnvelopeSignal.h`
   sha256 in the main tree (`5a36ef13798067f03af3101bac53416095762ce0fb07e738c5469fbfb88564e3`)
   matches the plan's pre-change pin exactly — confirms the mutation-check
   restore left it byte-identical.

2. NON-BLOCKING (verified): Independently rebuilt/ran
   `build-lane/tests/test_oscillator_bar_fold` in the builder's worktree —
   "All tests passed (500 assertions in 9 test cases)", matching the report.
   `grep -c TEST_CASE` confirms 9 (unchanged, as the plan requires — siblings
   live inside existing cases, no new TEST_CASE added).

3. NON-BLOCKING (verified): Independently re-derived the fold math for all
   three sibling blocks against `EnvelopeSignal.h:51-53`'s formula
   (`totalBeatPhase = beatPhase + beatInBar + 4*barsElapsed`,
   `cyclePhase = fmod(totalBeatPhase/beatDuration_, 1)`):
   - 8-beat sibling: peak (totalBarCount=1) → totalBeatPhase=4, cyclePhase=0.5
     → control point (0.5,1.0). Correct.
   - 16-beat sibling: peak (totalBarCount=2) → totalBeatPhase=8, cyclePhase=0.5
     → correct.
   - duration-4 sibling: for any integer `totalBarCount`, `4*tbc/4` is a whole
     number so `cyclePhase` is invariant — confirms the report's own claim
     that this sibling is a no-op guard, not a discriminator, and the
     comment in the diff says so explicitly (not overclaimed).

4. NON-BLOCKING (verified): Fail-first (T43) claim checked against
   `FeatureSnapshot.h:49-52` — both `barCount` and `totalBarCount` default to
   0, and none of the three sibling blocks' snapshots set `barCount`. Under
   the plan's mutation (`barsElapsed = snapshot.barCount` unconditional),
   the 8-beat and 16-beat peak assertions collapse to `cyclePhase=0 → value
   0.0`, correctly failing against the `WithinAbs(1.0f, 0.01f)` expectation —
   the old (pre-S168) behavior is genuinely probed, not a tautology. The
   duration-4 sibling passing under the same mutation is expected (see
   finding 3) and does not weaken the fail-first evidence for the other two.

5. NON-BLOCKING: Test-only change (Catch2 assertions), no production code,
   no audio-thread/RT path, no UI text — real-time-safety, thread-safety,
   and UI whole-word dimensions are not applicable to this lane.

6. NON-BLOCKING: Header-comment correction (`:38-42`) accurately reflects
   the new state and does not overclaim — it says the siblings were "added
   once a follow-up review named them," consistent with the addendum-5
   plan text.

## Scope vs spec

Matches `.harmony/specs/s-rta-0923-review-fixes-plan.md` lines 341-361
(L6 addendum 5) exactly: three sibling blocks with the specified snapshot
values, `envDefault` naming convention, `REQUIRE_FALSE(getResetPhaseOnStructural())`
guard, header-comment correction, and the mandatory mutation-check-with-restore
on the non-owned `EnvelopeSignal.h`. Acceptance criteria (green build, 9
TEST_CASEs, mutation evidence in report) all satisfied and independently
reproduced.

SUMMARY: 1 file reviewed (tests/test_oscillator_bar_fold.cpp), 0 blocking
issues, 6 informational/confirmatory findings. Independently rebuilt and ran
the target; independently re-derived the discriminating math; independently
verified the file-fence and the EnvelopeSignal.h restore hash.
