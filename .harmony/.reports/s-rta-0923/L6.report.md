# Lane L6 report — EnvelopeSignal default-mode siblings (addendum 5)

## Worktree / patch / report paths
- Worktree: `/Users/boriskarpman/projects/RealTimeAudio/.claude/worktrees/wf_b6c30536-17a-6`
- Patch: `/private/tmp/rta-patches/L6.patch`
- Stat: `/private/tmp/rta-patches/L6.stat.txt`
- Report: `/private/tmp/rta-patches/L6.report.md` (this file)

## What changed
File owned and modified: `tests/test_oscillator_bar_fold.cpp` only (no other file left
dirty — see mutation-check restore below). Added three default-mode
(`totalBarCount`) sibling blocks, one appended inside each of the three
pre-existing EnvelopeSignal `TEST_CASE`s that predate the `resetPhaseOnStructural_`
switch, following the same in-case pattern already used for the four
OscillatorSignal siblings earlier in the file:

1. **"EnvelopeSignal completes a full cycle over 8 beats (2 bars)"** (originally
   lines 306-334): appended `envDefault` (default `EnvelopeSignal("test", 8.0f)`,
   `REQUIRE_FALSE(envDefault.getResetPhaseOnStructural())`) with:
   - start (totalBarCount 0) -> 0.0
   - peak (beatPhase 0, beatInBar 0, totalBarCount 1) -> 1.0 +/- 0.01
   - nearEnd (0.99, 3, totalBarCount 1) < 0.1

2. **"EnvelopeSignal 16-beat option ... completes across 4 bars"** (originally
   lines 336-366): same pattern, `EnvelopeSignal("test", 16.0f)`:
   - start -> 0.0
   - peak (totalBarCount 2) -> 1.0 +/- 0.01
   - nearEnd (0.99, 3, totalBarCount 3) < 0.1

3. **"EnvelopeSignal default duration (4.0, the boundary case) is unchanged
   across bars"** (originally lines 368-387): swept `totalBarCount` in
   `{0, 1, 2, 5, 10}` against a `totalBarCount=0` baseline, default-constructed
   `EnvelopeSignal("test")` (beatDuration_ = 4.0f), `WithinAbs(expectedTbc,
   0.0005f)`. Comment explicitly notes this sibling is a no-op guard, not a
   discriminator, matching the spec's required annotation.

Also corrected the file's opening header comment (originally lines 38-42),
which previously stated the three EnvelopeSignal cases "were not given their
own totalBarCount siblings" — updated to state they now carry siblings, added
once a follow-up review named them for the same treatment as the
OscillatorSignal cases.

No other file was left modified. `git status --short` after restoring the
mutation-check edit shows only `tests/test_oscillator_bar_fold.cpp` as
modified.

## Fail-first evidence (mutation check on the file this lane does NOT own)

Per the plan, the mutation check edits `src/signal/EnvelopeSignal.h` locally
(a file L6 does not own) to prove the new siblings actually discriminate,
then restores it byte-identical.

1. **Pre-mutation hash** (must match plan's pin `5a36ef13...`):
   ```
   $ shasum -a 256 src/signal/EnvelopeSignal.h
   5a36ef13798067f03af3101bac53416095762ce0fb07e738c5469fbfb88564e3  src/signal/EnvelopeSignal.h
   ```
   Matched the plan's pinned hash exactly before any edit.

2. **Mutation applied** — changed lines 51-53 from the switch:
   ```cpp
   float barsElapsed = resetPhaseOnStructural_
       ? static_cast<float>(snapshot.barCount)
       : static_cast<float>(snapshot.totalBarCount);
   ```
   to the old unconditional legacy read:
   ```cpp
   float barsElapsed = static_cast<float>(snapshot.barCount);
   ```

3. **Rebuild + run against the mutated source**:
   ```
   $ cmake --build build-lane --target test_oscillator_bar_fold -j6
   [100%] Built target test_oscillator_bar_fold
   $ ./build-lane/tests/test_oscillator_bar_fold
   ...
   EnvelopeSignal completes a full cycle over 8 beats (2 bars)
   tests/test_oscillator_bar_fold.cpp:352: FAILED:
     REQUIRE_THAT( envDefault.getValue(peakTbc), WithinAbs(1.0f, 0.01f) )
   with expansion:
     0.0f is within 0.00999999977648258 of 1.0

   EnvelopeSignal 16-beat option ... completes across 4 bars
   tests/test_oscillator_bar_fold.cpp:410: FAILED:
     REQUIRE_THAT( envDefault.getValue(peakTbc), WithinAbs(1.0f, 0.01f) )
   with expansion:
     0.0f is within 0.00999999977648258 of 1.0

   test cases:   9 |   7 passed | 2 failed
   assertions: 498 | 496 passed | 2 failed
   ```
   Exactly matches the plan's predicted failure mode: "the 8-beat and 16-beat
   siblings FAIL (peak reads 0.0, barCount is 0 in those snapshots), the
   duration-4 sibling still passes (by design), the legacy blocks still
   pass." The duration-4 sibling (test case 3, "default duration (4.0)...")
   passed under the mutation as predicted (it's a no-op guard, not a
   discriminator, at that duration).

4. **Restored byte-identical**, re-hashed:
   ```
   $ shasum -a 256 src/signal/EnvelopeSignal.h
   5a36ef13798067f03af3101bac53416095762ce0fb07e738c5469fbfb88564e3  src/signal/EnvelopeSignal.h
   ```
   Matches the pin exactly (before and after are identical hashes).

5. **`git status --short`** after restore: only `tests/test_oscillator_bar_fold.cpp`
   shown as modified — `src/signal/EnvelopeSignal.h` is clean.

6. **Final rebuild + run against the restored (real) source** — confirms the
   restore didn't just match hash but also compiles/passes identically to
   the original pre-mutation state:
   ```
   $ cmake --build build-lane --target test_oscillator_bar_fold -j6
   [100%] Built target test_oscillator_bar_fold
   $ ./build-lane/tests/test_oscillator_bar_fold
   All tests passed (500 assertions in 9 test cases)
   ```

## Test counts
- Target: `test_oscillator_bar_fold` (Catch2 executable, built via
  `cmake --build build-lane --target test_oscillator_bar_fold`).
- Before this lane's change (baseline, not separately re-measured — the file
  already had 6 TEST_CASEs before the 3 EnvelopeSignal siblings existed per
  spec's count arithmetic; this lane only adds sibling assertions inside 3
  existing TEST_CASEs, not new TEST_CASEs).
- After this lane's change: **9 TEST_CASEs, 500 assertions, all green** —
  matches the plan's acceptance criterion "`test_oscillator_bar_fold` green;
  ctest count unchanged (306, siblings live inside existing cases; still 9
  TEST_CASEs in the file)."
- Did NOT run the full `ctest` suite in `build-lane` (only the one target was
  built, per the work packet's instruction to "Build only the test targets
  you need"). No other test binaries are affected by this change — the only
  file touched is `tests/test_oscillator_bar_fold.cpp`, and no other test
  file includes or depends on it.

## Anything NOT done
- Did not run the full 306+ ctest suite in this lane's build directory
  (out of scope per work packet: "Run every test binary your change could
  affect" — this change can only affect `test_oscillator_bar_fold`, which
  was run directly and is green).
- Did not touch `tests/CMakeLists.txt` (not owned by this lane; the target
  `test_oscillator_bar_fold` and its Catch2 link already existed).
- Did not attempt to serialize or address any of the OTHER lanes (L0-L5) —
  this lane has no dependencies per the plan ("No dependencies; can run
  first").

## Risks
- None identified specific to this change. The three new sibling blocks are
  pure test-side assertions with no production-code impact; the mutation
  check proves they exercise the real discriminating code path
  (`EnvelopeSignal.h:51-53`) and that the restore was exact (sha256-verified,
  `git status` clean on that file).
- The plan's risk #6 ("L6's mutation check edits a file the lane does not
  own") is addressed: restore + sha256 verification done and quoted above.

Not claiming "verified" as a final gate — Harmony gates this lane against
the merged tree.
