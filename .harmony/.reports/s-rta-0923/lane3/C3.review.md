# C3 Review (s-rta-0923, lane 3: connection-binding) — SOURCE review, independent (Reviewer did not build)

VERDICT: APPROVE-WITH-NITS

## Scope re-verified
- C3.patch applies cleanly on C0.patch (contract-only scaffold); file list matches plan section 5 C3 exactly: `src/MainComponent.h`, `src/MainComponent.cpp`, `src/api/ApiServer.h`, `src/api/ApiServer.cpp`, `src/ui/LayerStrip.cpp` (+314/-46, matches report's own `git diff --stat`).
- ManualWrite.h contract (C0) cross-checked against every C3 call site: `manualWriteCore(*ref, v, handFor(o,k), k, connNow(), composition_.gripHoldMs)`, `manualTouchCore(*ref, handFor(o,k), k, connNow(), composition_.gripHoldMs)`, `manualReleaseCore(*ref, handFor(o, GripKind::Held))` — argument order/types match the C0 header signatures exactly (VERIFIED by reading `ManualWrite.h` directly, not the report's claim).
- ScalarDef key strings: read `src/connect/ScalarParams.h` directly. `CompScalar`, `LayerScalar`, `ClipScalar` each define `{"opacity", ...}` verbatim — every one of C3's literal `"opacity"` `ControlPath::scalar` strings (compScalarPath/layerScalarPath/clipScalarPath call sites) matches the actual key resolveControl will string-match against. This is the one place a silent typo would have compiled fine and only failed at C5's live probe — checked and clean.
- Critic finding #1 (engine freezes twin instead of NaN-ing it during a grip) is a C1 defect, not C3's — read `C1.patch` directly: `live[i].v.store(std::isnan(y) ? kNan : ...)` replaces the old `if (!std::isnan(y)) store(...)` pattern at all publish sites (scalars, effect params, dryWet, macro `currentValue`) — the fix is present, so C3's `addLiveBlock`/`eff()` reads will behave as C3 assumed once C1 merges. Not a C3 defect either way.
- Critic finding #4 (`manualTouch` seam for the future recorder `Sink::touch`) is implemented exactly as specified: `MainComponent::manualTouch` wraps `manualTouchCore`, `onManualTouch` hook fires with the core's accepted bool, header comment cites the amendment.

## Thread safety
- `tickFeaturePipeline()` is a JUCE `Timer::timerCallback` override (confirmed: `MainComponent.h:264`) → message thread, not the audio/analysis/render threads. `ConnectionEngine::tick()` being called there is correctly outside the RT-sacred paths; no sacred-rule violation.
- `manualWrite`/`manualTouch` both assert `juce::MessageManager::getInstance()->isThisTheMessageThread()`. Every wired call site was traced to confirm it actually runs there: OSC callbacks (pre-existing `MessageLoopCallback` dispatch per CLAUDE.md), MIDI binding action (pre-existing `MidiHandler.cpp` already wraps `handleIncomingMidiMessage` in `juce::MessageManager::callAsync` before calling into binding dispatch — read directly, not assumed), and the two REST callbacks (both fire from inside pre-existing `juce::MessageManager::callAsync` blocks in `ApiServer.cpp`). All consistent — no thread-assert violation on any of the 11 sites.
- `LayerStrip`'s `onDragStart`/`onDragEnd` grip calls are JUCE UI callbacks → message thread; consistent with the rest.

## Nits (NON-BLOCKING)

1. **`manualRelease` has no message-thread jassert**, unlike its two siblings `manualWrite`/`manualTouch` (`MainComponent.cpp` new code, ~line 246). It is not called from anywhere in this lane's own patch (none of the 11 sites has a release event by design — only LayerStrip's direct widget grip does, and it bypasses the funnel entirely per plan). Harmless today, but it's public API surface (`onManualRelease` hook, called by step-3/C4 later) that silently permits off-thread misuse where its siblings would assert. Suggest matching the guard when it gets its first real caller.
2. **`manualWrite`'s bool return is discarded at all 11 call sites** — refusals (a Held grip outranking a Decaying OSC/MIDI/REST write) are silent by design (D8 rank arbitration), not logged anywhere. Matches the spec's intended behavior; flagging only because there's currently zero diagnostic visibility if a user reports "my MIDI knob did nothing" while dragging a UI slider.
3. **Two documented, disk-justified plan deviations** (NUANCE section of the report): comp-level `live` JSON uses `ScalarDef::key` ("opacity"/"speed") rather than the plan prose's literal `"masterOpacity"`/`"masterSpeed"` wording, matched instead to C0's already-committed `probe-lane3.sh` (`d['live']['positionX']` etc. — re-read directly, confirms the report's claim: the probe does expect `ScalarDef::key` shape). `onSetClipEffectParam` gained a `paramName` parameter beyond the plan's literal table, needed so `clipParamPath`'s D2 name-resolution field isn't left empty/lossy. Both are reasonable, narrow, and correctly justified against the actual gating artifact (the probe script) rather than against prose — but they are still deviations from written spec text, so flagging for Harmony to confirm intent before C5 scores it.
4. **`manualRelease` is unreachable/uncovered in this commit** — defined, exported via the header, never invoked. Not a defect (matches the plan: no release events among the 11 sites), but means this method's correctness is unverified until a future lane (recorder or C4) exercises it.

## Not this lane's scope (confirmed by grep, not re-reviewed)
C2's renderer `eff()` repoint, C4's 21-widget binding + ConnPicker, C1's `ManualWrite.cpp` bodies (already spot-checked above for the NaN fix relevant to C3's assumptions) — none touched by this patch; report's own UNKNOWNS section names these accurately.

## Summary
No BLOCKING findings. The funnel wiring, all 11 writer-site routings, the engine-tick insertion point, and the `live`/`connected` API block all match the plan's contract (as amended by the critic) and C0's/C1's actual header/behavior — verified by reading the real headers and C1's actual diff, not by trusting the report's prose. Thread-threading of every call site was independently traced to the message thread. The two plan-text deviations are narrow, justified against the disk-verified probe script, and called out explicitly by the builder — Harmony should give a one-line thumbs-up on those two before C5 runs, but neither blocks merge.
