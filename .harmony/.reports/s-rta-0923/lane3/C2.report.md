# Lane C2 — renderer read repoint report (s-rta-0923, lane 3)

STATUS: DONE

## RESULT

Repointed every raw read of the 22 connection-backed scalar fields in
`src/render/CompositorEngine.cpp` and `src/render/Renderer.cpp` to
`Clip::eff()`/`Layer::eff()`/`Composition::eff()` per plan section 4.4.
Committed worktree-local (never pushed) at `cfc2f504941b895932029b0dcf9adb029e2ca42b`
in worktree `/Users/boriskarpman/projects/RealTimeAudio/.claude/worktrees/wf_f21c0ef9-872-4`,
on top of C0's scaffold commit (`70aacfe`, cherry-picked via
`git am /private/tmp/rta-patches/lane3/C0.patch`). Patch (this commit only,
NOT C0's): `/private/tmp/rta-patches/lane3/C2.patch`.

## FACTS (disk-cited)

- Worktree HEAD before work: `dd646de` (one behind main's `bb69c4c`). Fast-forwarded
  via `git merge --ff-only main` (not `git reset --hard main` — the stash-guard
  PreToolUse hook unconditionally blocks `reset --hard` from an agent when
  Harmony_Main, the hook's own dirty/peer-check target, is dirty or another
  session is live, which it was during this dispatch; a fast-forward merge reaches
  the identical tree with zero destructive potential since the worktree itself was
  clean — confirmed via `git status --short` returning nothing before the merge —
  and `merge --ff-only` is not a guarded tree-wide op). `git log -1 --oneline`
  after: `bb69c4c chore(s-rta-0923): drop a build log...`.
- `git am /private/tmp/rta-patches/lane3/C0.patch` applied cleanly, one commit
  (`70aacfe`, "C0 scaffold (worktree-local)"), 14 files, +1126/-0.
- This lane's diff: 2 files changed, 70 insertions(+), 32 deletions(-)
  (`git diff --stat` on the two target files before commit).
- Grep gate (plan §4.4's exact command) on the final tree:
  `grep -n "clip\.positionX\|clip\.positionY\|clip\.scale\b\|clip\.rotation\|clip\.anchor\|clip\.clipOpacity\|clipOpacity\|layer\.opacity\|layer\.positionX\|layer\.positionY\|layer\.layerScale\|layer\.layerRotation\|layer\.layerAnchor\|composition_->masterOpacity\|composition_->masterSpeed\|composition_->comp" src/render/CompositorEngine.cpp src/render/Renderer.cpp`
  returns 6 lines, all comment lines, plus one non-field-read false positive
  (`Renderer.cpp:1410`, `compile("clip_opacity_blend", EmbeddedShaders::clipOpacityBlend);`
  — a shader-registration call using a C++ symbol name that happens to contain
  the substring "clipOpacity"; it is not a struct-field read of any kind, so it
  is not in scope for this lane and was left untouched). Full grep output
  quoted below in TESTS.
- Full `AudioDNA` app target built clean in a scratch `build-lane` (removed
  after verification, 283MB): `cmake --build build-lane --target AudioDNA -j10`
  exits 0, links, codesigns. Re-touched both changed files and rebuilt to
  isolate their own warnings: zero new warnings in `Renderer.cpp`; one
  warning in `CompositorEngine.cpp` at line 1504 (`unused variable 'totalCells'`)
  — confirmed pre-existing and unrelated: `git diff` for this lane never
  touches that line (it's inside `applyScreenSplit`, ~900 lines from the
  nearest edit), and the line was present before this lane's changes.
- `test_compositor` (GL-free regression, mirrors `combinedOpacity`/
  `effectiveClipSpeed`): built and run from `build-lane/tests/test_compositor`
  — "All tests passed (42 assertions in 8 test cases)", unchanged from the
  pre-lane baseline (this target does not touch GL, so it exercises the model
  layer only — it was not expected to catch a bad repoint, per the plan's own
  §8 rejection of "test the renderer repoint with a GL-free mirror test", but
  its continued green status confirms this lane didn't break anything it
  covers).
- App was NOT launched (forbidden to this lane; C5/Harmony's job).

## METHOD

Read the governing plan (`/Users/boriskarpman/projects/RealTimeAudio/.harmony/specs/s-rta-0923-lane3-plan.md`,
753 lines, read from the non-worktree project dir per C0's own report — it is
untracked there, not present in the worktree) end to end, focusing on §1 (scout
recon delta), §2 (the 21-control table with exact anchors), §4.4 (C2's own
piece), §5 (C2's lane definition and DONE criteria), and §9 (R-E, R-J risks
naming C2 specifically). Also read `.harmony/scout-lane3-connect-surface.md`
(the original recon this plan is built on) for the full architectural picture
of why the renderer reads raw fields today. Read every touched model file
(`src/connect/ScalarParams.h`, `src/connect/LiveValue.h`, `src/model/Composition.h`,
`src/model/Layer.h`/`.cpp`, `src/model/Clip.h`/`.cpp`) to confirm `eff()` is
already implemented (not a C2 dependency — it's pre-existing, unit-tested
infrastructure from an earlier lane) and that `ClipScalar`/`LayerScalar`/
`CompScalar` are visible unqualified from both target files' existing include
chains (`CompositorEngine.h` → `model/Deck.h` → `Layer.h`/`Clip.h` → `connect/
ScalarParams.h`; `Renderer.h` → `model/Clip.h` → same). Read the full body of
every function the plan's own anchor table names (`applyClipTransform`,
`applyLayerTransform`, `applyFXOnlyLayer`, `compositeDeck`, `compositePersistentLayers`,
`applyLayerKeying`, `blendLayerOntoAccumulator`, `applyTransition`,
`renderOpenGL`, `getVideoFrameTexture`, `applyCompTransform`) before editing
any of them, then ran the plan's own grep gate BEFORE editing to get the
authoritative site list (matched the plan's anchor table exactly, with line
numbers shifted only by pre-existing unrelated code above). Edited each site
as a local-variable read-through (`const float effX = owner.eff(Scalar::X);`)
computed once per function/scope and reused at every use site in that scope,
matching the codebase's own existing naming convention (`effOpacity` was
already a local-variable name in `applyFXOnlyLayer` before this lane touched
it). Re-ran the same grep gate after editing to confirm closure. Configured a
scratch CMake build (`build-lane`, per the packet's own instruction) pointed
at the main tree's already-fetched `_deps` to avoid re-downloading JUCE/Catch2/
httplib/melatonin_inspector/syphon; built the full `AudioDNA` app target (this
lane has no dedicated test target — GL code, per plan §5's "No new test (GL)"
— so the app build itself is the compile/link proof); touched both changed
files and rebuilt to isolate their own warnings from the ~250 pre-existing
JUCE-library warnings; built and ran `test_compositor` as the plan's named
regression proof; deleted `build-lane` after verification.

## CONFIDENCE + VERIFY

HIGH on compile/link/grep-gate/test_compositor claims — all re-derived by
building and running, not asserted from reading. HIGH on completeness of the
site list — cross-checked the plan's own anchor table (§4.4) against a fresh
grep before AND after editing, and both matched exactly (7 clip sites, 6
layer-transform sites, 1 combinedOpacity call, 2 opaque-branch opacity sites,
1 keying-opacity site, 1 transition-opacity site, 2 masterSpeed sites, 1
masterOpacity site, 6 compTransform sites = 27 individual read expressions
across 12 distinct call sites). MEDIUM on whether `compositePersistentLayers`
(lines 934-1006) needed any C2 changes: it calls `blendLayerOntoAccumulator`
and `applyLayerKeying` (both already repointed as shared helpers) but does NOT
call `applyClipTransform`/`applyLayerTransform` at all — persistent layers
render without per-clip/per-layer transform today. This is a pre-existing gap
in `compositePersistentLayers` itself (it never applied transforms, connected
or not), not a raw-field-read this lane's grep gate would catch, and adding
transform calls there would be new behavior outside "repoint existing reads" —
flagged in ISSUES rather than silently expanded into. Re-verify: run
`.harmony/probe-lane3.sh` (C5, Harmony's) once C1/C3/C4 all land; the grep gate
above can be re-run standalone at any point to re-prove this lane's own claim
without a build.

## UNKNOWNS / NOT DONE

- Did not touch `compositePersistentLayers`'s missing transform calls (see
  CONFIDENCE above) — out of this lane's stated scope (repoint EXISTING raw
  reads; there is no raw transform read there to repoint, since no transform
  is applied there at all today).
- Did not run `.harmony/probe-lane3.sh` (forbidden to this lane; C5/Harmony's
  job) — per plan §9 R-J, this lane's repoint "cannot fail headlessly (GL/app)";
  its fail-first proof is C5's pixels, not a builder-run test.
- Did not build or run `test_manual_write`/`test_conn_picker` (C0/C1/C4's
  targets, not touched by this lane's files) — irrelevant to C2's own proof.

## NUANCE

- The plan's own grep gate string has one false-positive substring match
  (`clipOpacity` inside `EmbeddedShaders::clipOpacityBlend`, a shader-program
  registration call, `Renderer.cpp:1410`) that is not a field read at all —
  flagged explicitly rather than silently "fixed" by renaming the shader
  symbol (out of this lane's scope and would touch `EmbeddedShaders.h`, not a
  C2 file).
- `applyLayerTransform`'s early return (`if (!needsTransform) return srcTex;`)
  and `applyClipTransform`'s `needsTransform` gate are now both computed from
  `eff()` reads, so a connection driving any of Position/Scale/Rotation
  correctly forces the transform pass to run even when the RAW manual field
  sits at its default — this is the exact mechanism plan §9 R-E calls out for
  `applyCompTransform`'s `isDefault` early return, and the same reasoning
  applies symmetrically to these two per-clip/per-layer gates (not explicitly
  named in R-E, which only discusses `applyCompTransform`, but the same
  class of correctness property).
- `combinedOpacity(layer.eff(...), clip.eff(...))` in `applyFXOnlyLayer`
  reads BOTH scalars fresh via `eff()` rather than reusing a value computed
  elsewhere — there is no shared local across functions to thread through
  (each of the 12 call sites is a separate function with its own `Clip&`/
  `Layer&` reference), so each site does its own single `eff()` call(s); no
  redundant GL-thread work beyond what the raw-field version already did
  (same number of reads, same cost class — an atomic relaxed load vs a plain
  float load).

## HANDOFF-NEEDS

None for C2 itself (scope complete, no dependency on other lanes to compile
or link — `eff()` is pre-existing infrastructure, not something C1/C3/C4
create). For C5 (Harmony): once C1 (funnel core), C3 (engine tick + external
writer sites), and C4 (widget binding) all land, this lane's repoint is what
makes the live pixels move — C5's Oracle B (render_frame md5 differs between
live=1/live=0) is this lane's actual proof, not anything a builder can run
headlessly (plan §9 R-J).

---

## SUMMARY

Lane C2 (renderer read repoint) for s-rta-0923's connection-binding lane 3:
the GL-thread half of "wire the 21 dead controls" — every raw read of the 22
connection-backed scalar backing fields in `CompositorEngine.cpp`/
`Renderer.cpp` now reads through `eff()` (the atomic `LiveValue` twin with
manual-field fallback) instead of the raw field directly. Zero behavior
change for any currently-unconnected scalar (the twin holds NaN, `effective()`
falls back to the exact same manual field read as before); once C1/C3/C4 wire
registration + engine tick + the funnel, a connected scalar's driven value
now actually reaches the shader uniforms/GL calls instead of being silently
ignored.

## FILES CHANGED

- `src/render/CompositorEngine.cpp` — 12 read sites across 6 functions:
  `applyClipTransform` (7 `ClipScalar` reads: PosX/PosY/Scale/Rotation/
  AnchorX/AnchorY/Opacity), `applyLayerTransform` (6 `LayerScalar` reads:
  PosX/PosY/Scale/Rotation/AnchorX/AnchorY), `applyFXOnlyLayer` (1
  `combinedOpacity` call, 2 `eff()` reads inline), `compositeDeck`'s Opaque
  branch (1 local `effLayerOpacity`, used at 2 call sites), `applyLayerKeying`
  (1 `u_opacity` read), `applyTransition` (1 outgoing-clip opacity read).
- `src/render/Renderer.cpp` — 4 sites: `renderOpenGL`'s `masterSpeedVal`
  (line ~415) and `masterOpacityVal` (line ~706), `getVideoFrameTexture`'s
  `masterSpeedVal` (line ~1241), `applyCompTransform`'s 6 `CompScalar` reads
  (posX/posY/scale/rotation/anchorX/anchorY, lines ~2021-2026).

## TESTS

Grep gate (plan §4.4's exact command), final tree:
```
$ grep -n "clip\.positionX\|clip\.positionY\|clip\.scale\b\|clip\.rotation\|clip\.anchor\|clip\.clipOpacity\|clipOpacity\|layer\.opacity\|layer\.positionX\|layer\.positionY\|layer\.layerScale\|layer\.layerRotation\|layer\.layerAnchor\|composition_->masterOpacity\|composition_->masterSpeed\|composition_->comp" src/render/CompositorEngine.cpp src/render/Renderer.cpp
src/render/CompositorEngine.cpp:496:    // S167-L4b: bake clipOpacity in here (rather than only at the final
src/render/CompositorEngine.cpp:523:    // layer left at the Opaque type with layer.opacity at its 1.0 default
src/render/CompositorEngine.cpp:527:    // the same technique masterOpacity and layer.opacity's own Opaque-path
src/render/CompositorEngine.cpp:530:    // correctly with layer.opacity's alpha-consuming Normal/Additive blend
src/render/CompositorEngine.cpp:623:    // below 1.0 dilutes the FX-Only blend even further than layer.opacity
src/render/Renderer.cpp:1410:    compile("clip_opacity_blend",   EmbeddedShaders::clipOpacityBlend);
```
5/6 lines are comments (the gate's own success criterion); the 6th
(`Renderer.cpp:1410`) is a shader-registration symbol name, not a field read
(see NUANCE).

Build (scratch, removed after):
```
cmake -S . -B build-lane -DCMAKE_BUILD_TYPE=Release \
  -DFETCHCONTENT_SOURCE_DIR_JUCE=.../build/_deps/juce-src \
  -DFETCHCONTENT_SOURCE_DIR_CATCH2=.../build/_deps/catch2-src \
  -DFETCHCONTENT_SOURCE_DIR_HTTPLIB=.../build/_deps/httplib-src \
  -DFETCHCONTENT_SOURCE_DIR_MELATONIN_INSPECTOR=.../build/_deps/melatonin_inspector-src \
  -DFETCHCONTENT_SOURCE_DIR_SYPHON=.../build/_deps/syphon-src
cmake --build build-lane --target AudioDNA -j10     # exits 0, links, codesigns
cmake --build build-lane --target test_compositor -j10   # builds clean
./build-lane/tests/test_compositor
  Randomness seeded to: 2193508290
  ===============================================================================
  All tests passed (42 assertions in 8 test cases)
```

## ISSUES

None blocking. One observation for whoever eventually touches
`compositePersistentLayers` (not this lane, not named in plan §5's C2 file
list): it composites persistent layers without ever calling
`applyClipTransform`/`applyLayerTransform` at all, connected or not — a
pre-existing gap, not something this lane's grep-gate-scoped repoint covers
or should silently expand into fixing.

## SKILL_PROPOSALS

None — this followed the existing multi-lane dispatch pattern
(`superpowers:dispatching-parallel-agents`-shaped), no new reusable procedure
discovered.

## RISKS

- None specific to this lane's own correctness (mechanical repoint, verified
  by grep gate + successful app build + unchanged `test_compositor`). The
  live-pixel risk (whether a driven value actually looks right on screen) is
  explicitly C5/Harmony's proof, not something headlessly provable — plan §9
  R-J states this outright for all of C2/C3/C4.
- If C1's `Grip::rank`/`ConnectionEngine::tick` fix (critic finding #1 — the
  engine currently leaves the twin at the last-published LFO value during a
  grip, per the dispatch's CRITIC block) is NOT yet landed when this patch is
  merged, the `eff()` reads this lane added will read stale/frozen twin
  values during a grip exactly as finding #1 describes — that is a C1 defect
  this lane's reads would faithfully surface, not something C2 introduces or
  can fix from its own file scope.

## METRICS

Session duration: this dispatch only (single-lane repoint, plus C0 cherry-pick
setup). Files touched: 2 (both modified, both plan-listed for C2). Lines: this
lane's own diff +70/-32 (2 files). Scratch build size: 283MB (removed).

## PACKET QUALITY

- Clarity: CLEAR. Plan §4.4's anchor table matched the actual source
  line-for-line (only shifted by pre-existing unrelated commits between the
  plan's authoring HEAD and this dispatch's HEAD, which the plan itself
  warned about: "re-grep the anchor, never trust the line"). The critic's
  BLOCKING findings (1-5) were all about C0/C1/C3/C4's file scope, not C2's —
  none required a C2-specific amendment; re-read all five to confirm none
  touched `CompositorEngine.cpp`/`Renderer.cpp` before proceeding.
- Missing context: none. The plan's §4.4 table, §5 C2 section, and §9 R-E/R-J
  risks together fully specified this lane's scope, proof, and known
  limitations before any editing began.
- Unused context: the critic's BLOCKING findings 1-5 (engine grip-NaN bug,
  CMakeLists.txt ownership, Grip::rank scaffolding, the touch/Sink seam,
  C0's fail-first refusal cases) are all C0/C1/C3/C4 concerns — read in full
  to confirm no C2 overlap, then correctly not acted on here.
- Self-brief files: none listed in this packet (no SELF-BRIEF section); read
  `CLAUDE.md` (already supplied in context), the plan, the scout recon, and
  every touched model/render header directly instead, matching C0's own
  precedent for this lane.

## STATUS

DONE

## NEXT ACTION

Harmony merges C1 (funnel core) → C2 (this lane) → C3 (MainComponent/ApiServer/
LayerStrip wiring) → C4 (widget/inspector binding), per plan §5's stated merge
order, into `build/` for a clean forced rebuild + `ctest`, then dispatches C5
(herself) to run `.harmony/probe-lane3.sh` once against the pre-change binary
(expect FAIL at "connected contains opacity") and once against the fully
merged tree (expect all PASS, per plan §6).
