# Reviewer Verdict — Lane 3 / C4 (s-rta-0923)

VERDICT: APPROVE

## Scope / fence
Diff-stat (`git diff --stat 7dd0968 759032c`) touches exactly the 13 files the
plan names: C4's 9 (`ConnPicker.cpp`, `ClipInspector.{h,cpp}`,
`CompositionInspector.{h,cpp}`, `LayerInspector.{h,cpp}`,
`UniversalParamControl.{h,cpp}`) + section 4.6's 4
(`Composition.h`, `TestServer.cpp`, `test_composition.cpp`,
`test_composition_tier_oracle.cpp`). Zero files outside the fence. Two
commits, section-4.6 isolated in its own commit as required.

## Independent re-verification (not taken from the builder report)
- Configured a scratch `build-review` against the main tree's cached `_deps`
  (no re-download), `-DAUDIODNA_BUILD_TEST_SERVER=ON`.
- Built + ran `test_conn_picker` (23/23 assertions), `test_composition`
  (287/287), `test_composition_tier_oracle` (25/25), `test_connection`
  (116/116) — all green, matching the report's numbers exactly.
- Built the FULL `AudioDNA` app target from clean (not just `-c` compile-only)
  — exit 0, linked, codesigned. This is strictly stronger evidence than the
  report's compile-only technique and confirms `ConnPicker.cpp`,
  `UniversalParamControl.cpp`, all 3 inspectors, and `TestServer.cpp` all
  link into the real app target.
- Deleted the scratch build dir after verification; worktree confirmed clean.

## Fail-first (T43)
Read C0's actual stub at `7dd0968:src/connect/ConnPicker.cpp`:
`sourceFromPicker` unconditionally returns `ConnSource{}` (`Kind::None`),
`describeSource` unconditionally returns `""`. `tests/test_conn_picker.cpp`
was authored by C0 (untouched by C4's diff) and asserts non-`None` kinds /
non-empty descriptions in 6 of 8 cases — those necessarily failed against
the stub; the 2 `[pin]` cases (Manual→None, None→"") pass by construction.
Genuine fail-first, not fabricated.

## Thread safety / RT
`UniversalParamControl` is message-thread-only (JUCE UI). It writes
`conn_->shape.{outMin,outMax,inverted}` and `conn_->source` directly with no
atomics. Traced the only reader: `ConnectionEngine::tick()` is wired by C3
into `MainComponent::tickFeaturePipeline()`, which fires from a
`juce::Timer` (`MappingTickTimer`, `MainComponent.h:260-264`) — i.e. the
message thread, the same thread as every `UniversalParamControl` write. No
cross-thread race. The GL thread only ever touches the atomic `LiveValue`
twin via `eff()`, never raw `ParamConnection` fields — consistent with the
plan's own thread model (§4.4). No RT-audio-thread code touched.

## Correctness spot-checks
- `ScalarDef` formulas in `ScalarParams.h` (posX3840/posY2160/expScale/
  rot720/linScale2/speed4 and their `normX` inverses) match the raw
  formulas each `syncScalar` lambda's `todayFormula` fallback uses,
  verbatim — the "thumb follows signal" repoint doesn't silently change
  unconnected-control behavior.
- Macro menu-id fix (`result < 200 + MacroBank::kNumMacros`) matches the
  menu-build loop at `UniversalParamControl.cpp:495-503`
  (`kNumMacros = 8`, `MacroBank.h:17`) — the old `< 206` bound genuinely
  dropped Macro 7/8 from ever being reachable; `test_conn_picker`'s
  "Macro 7 is reachable" case pins the fix.
- `haveChoice` guard in `handleSourcePickerResult`: a failed
  `signalRegistry_->getSignalAt(idx)` lookup now leaves `*conn_` untouched
  instead of silently disconnecting — a real defensive correctness fix,
  not a plan deviation (the plan's `PickerChoice`/`ConnPicker` contract has
  no representation for "lookup failed").
- R-C mitigation (held grip released on widget destruction / rebind) is
  present in both `~UniversalParamControl()` and `bindConnection()`,
  exactly as the plan's risk register assigns to C4.
- `compOpacity` deletion (section 4.6): field, `toVar`/`fromVar` guard,
  `TestServer.cpp` branches, and both test files' coverage all removed
  consistently; old presets still load (`hasProperty`-guarded). One
  remaining `compOpacity` grep hit is a comment in
  `src/connect/ScalarParams.h`, a file outside both C4's and 4.6's file
  list in the plan — correctly left untouched (Iron Law #1), and honestly
  flagged in the builder's report rather than silently left for the
  reviewer to find.

## UI whole-words
All new/changed display strings (`describeSource`'s "Sample Hold", "Clip
Position", "Timeline", "Macro N", the BPM-sync "1/4 Beat"/"8 Beats" labels)
are whole words and are pulled from the widget's own existing menu-string
tables (`shapes[]`/`divisions[]` in `UniversalParamControl.cpp`) rather than
independently authored — no drift risk between picker and translator.

## Non-blocking
1. The builder's own MEDIUM-confidence disclosure (meter-row/picker
   checkmark lag behind a freshly-bound already-connected control until the
   picker is touched once) is real but cosmetic, explicitly scoped by the
   plan's literal "existing paint code stays as is" (§4.2), and honestly
   flagged rather than hidden. No action needed for this lane; worth a
   follow-up polish ticket if Boris notices it live.
2. `ParamConnection::grip` is released via `release(connNow())` in the
   widget destructor unconditionally when `grip.kind != None` — correct per
   plan, but note this is a HumanHeld/Decaying release regardless of which
   `Hand` rank actually holds the grip (a lane's grip, if one were ever
   present here, would also be released). Not reachable in practice today
   (Player/Lane grips don't exist until step 3), so not a bug now — noted
   for whoever builds step 3's `Sink::touch`.

No blocking findings.
