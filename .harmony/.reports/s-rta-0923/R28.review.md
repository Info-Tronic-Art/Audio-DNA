# R28 Independent Review

VERDICT: APPROVE-WITH-NITS

## Scope / file-fence
Patch touches exactly: CLAUDE.md, CMakeLists.txt, src/recording/AudioStore.{h,cpp} (new),
src/recording/AudioTap.{h,cpp}, src/recording/Take.{h,cpp}, tests/CMakeLists.txt,
tests/test_audio_tap_sync.cpp, tests/test_audio_store.cpp (new), tests/test_take_v1_transport.cpp (new),
tests/fixtures/{take_v3_audio,take_v2_legacy_audio,take_v1_transport}.json (new).
All within spec §10.1's "Owns" list. No touch to test_take.cpp, RecorderClock.*, Player.*, Program.*,
TempoMap.*, PerformanceRecorder.*, src/connect/, src/ui/ — VERIFIED (grep of patch file list). No fence
violation.

## Correctness vs spec (VERIFIED by reading AudioStore.cpp/.h against spec §§1-7 line by line)

1. D-A10 (flush): `setFlushInterval(rate_*kHeaderFlushSeconds)` placed immediately after
   `ThreadedWriter` construction (AudioTap.cpp:126-131), matches spec §4.4 exactly.
2. D-A11 (asset always filled): `finalize()` fills `asset` from the WAV header when readable,
   else from `facts` with `fingerprint=""` — matches. One edge case not literally spec'd: a
   *readable* WAV that fails to fingerprint (float; never happens in production since AudioTap
   only writes 16-bit) leaves `wavReadable=true` but no sidecar written (fingerprint empty) —
   consistent with "never write a sidecar with an empty fingerprint," not a bug, just an
   unexercised-in-production branch.
3. D-A12 (ResolvedUnverified): resolve() step (7) matches spec's `seg.fingerprint.empty()` branch
   exactly, including the reason text carrying the sidecar's fp1.
4. D-A13 (abandonAsset): preconditions (`id == activeAssetId_` AND no sidecar) match; refuses
   correctly in all three test-11 negative cases (second call, complete asset, non-active id).
5. `resolve()` 8-step ladder (AudioStore.cpp:380-513) matches spec order (1)-(8) verdict-for-verdict,
   including the "no crash on B2's deref" case (mode set, no segments → Mismatch not a null-deref).
6. `fp1` (AudioStore::fingerprint, :538-592): header string format, window logic (one window if
   N<=2W else `[0,W)`+`[N-W,W)`), int-read via `reader->read(int* const*, ...)`, `>>16` PCM16
   extraction, `SHA256` — all match spec §3 verbatim, including the float-WAV → nullopt guard.
7. `referencing()`/`CaptureFacts::fromAudioRef()` domain conversion (D-A9) — round-trips exactly;
   test 13 confirms via a direct computation independent of the store's own code path.
8. `scanTakes`/`unreferencedAssetIds` (GC query only, D-A8) — the dangling-pointer fix (`const
   juce::var parsed` kept alive across the iteration) is real and matches the pattern already used
   elsewhere in this codebase (Lane.h's DiscretePoint::toVar comment, cited in the builder report);
   confirmed present in the shipped code, not just claimed.
9. Take.cpp L1 review-fix (`fromV1Var`'s TransportChange case, :393-413) — read directly: the
   `continue` inside a range-based `for (const auto& ev : *eventArray)` loop safely skips seq-mint
   and lane insertion for dropped events; `p.s.seq = take.nextSeq++` sits after the switch
   (:429), confirmed only reached on the non-`continue` path. Matches spec's L1 block verbatim.
10. CMake fences (§4.3): root CMakeLists.txt gets exactly the AudioStore source pair + 
    juce_cryptography; tests/CMakeLists.txt is appended-only after the pre-existing EOF (confirmed:
    diff hunk is `@@ -671,3 +671,85 @@`, i.e., only adds, touches nothing existing).

## Real-time / thread safety
`AudioStore` is message-thread-only blocking file I/O (same posture as AudioTap::start/stop) —
no audio-thread interaction. `setFlushInterval` is called once, synchronously, on the message
thread right after `ThreadedWriter` construction inside `AudioTap::start()` (message thread only,
confirmed by class-level threading contract comment in AudioTap.h) — the actual flush executes
on the writer's own `TimeSliceThread`, never the audio thread; `push()`'s RT path is untouched
(diff shows zero lines changed inside push()). No new mutexes, no heap allocation on a hot path.

## Fail-first proof — inspected critically (T43)
The flush test (`[audiotap][flush]`, test_audio_tap_sync.cpp:2169-2272) polls a *fresh* WAV
reader every 10ms for up to 5s until the header reports nonzero length. Pre-fix, `ThreadedWriter`'s
`samplesPerFlush` defaults to 0 (never flushes except at destruction, cited in the spec from JUCE
source), so `REQUIRE(sawFlush)` genuinely times out and fails — this is a real behavioral probe of
the OLD code, not a tautology; the test only synthesizes 4 blocks and relies on the background
TimeSliceThread having *something* to flush, which is true regardless of the fix (the interval
gate is what's being tested, not data availability). Report's mutation-check reproduces this
(`false` / test cases: 1 | 1 failed) and matches independent reasoning here.

The L1 test (test_take_v1_transport.cpp) checks exact point counts (3 audio points, not 5),
`p.v==0` (not the old `value*1000`), and `v1Dropped` counts — the report's described mutation
(revert only the switch-case body) would restore the old `value*1000` scaling and the missing
drop/count logic, producing `5==3 FAILED` / `6==4 FAILED` exactly as claimed. Plausible and
consistent with the code read here.

## Silent-failure paths
None found beyond the one documented, intentional exception above (unfingerprinted-but-readable
WAV → sidecar withheld, per "never write empty-fingerprint sidecar"). `resolve()` never silently
downgrades to a laxer status; every branch either returns a named `Status` with a `reason` or
falls through to the next check — no default/fallthrough path returns `Resolved` by omission.

## UI whole-word rule
N/A — no UI files touched by this lane.

## Nits (non-blocking)
1. Report's own flag: spec §10.1's stated "+19" acceptance delta is arithmetically "+20"
   (306-1+1+18+2=326, a delta of 20 not 19) — correctly identified as a spec typo, not a
   builder error; Harmony's final gate should state the number it measures, as the report says.
2. CLAUDE.md's new "Audio Store (Ruling 28)" note is one long paragraph rather than a hard-wrapped
   6-line block — spec said "6-line note"; builder read this as "short" and flagged the ambiguity
   itself. Cosmetic only.
3. `AudioAsset::toVar()`/`fromVar()` and `AudioStore::resolve()`'s inline sidecar-parsing duplicate
   the "parse JSON, get DynamicObject" pattern in three places (AudioStore.cpp `find()`, `resolve()`
   step 4) without extracting a shared helper — minor DRY nit, not worth blocking on for a first
   landing of a new subsystem.

## Findings
1. NON-BLOCKING — spec's own "+19" delta arithmetic is off by one (report already flags this;
   Harmony's gate should re-measure, not trust either number blindly).
2. NON-BLOCKING — CLAUDE.md note is a paragraph, not literally "6 lines"; readable either way.
3. NON-BLOCKING — minor JSON-parse duplication in AudioStore.cpp; no correctness impact.

No BLOCKING findings. Spec fidelity, file-fence discipline, RT/thread safety, and the two
fail-first behavioral proofs all check out against direct reading of the shipped diff.
