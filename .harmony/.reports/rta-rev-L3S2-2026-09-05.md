# Reviewer Verdict — rta-rev-L3S2-2026-09-05
STATUS: DONE
VERDICT: PASS

FILES REVIEWED: src/MainComponent.cpp, src/MainComponent.h, src/ui/CompDecksBrowser.h,
src/core/CompositionLoad.h, tests/test_composition.cpp (all 5 fenced files, full diff read
line-by-line; cross-checked against DeckView.cpp, LayerStrip.cpp/.h, ClipCell.cpp/.h,
Renderer.cpp, ImageSequence.cpp, Composition.h for every raw-pointer/UAF claim below).

## Adjudication, in the order requested

1. DANGLING-INSPECTOR CRASH — CLOSED, on every path, plus a SECOND independent UAF
   class the builder found and fixed beyond what the packet specified.
   - ClipInspector::setClip(nullptr) / LayerInspector::setLayer(nullptr) run FIRST inside
     refreshUiAfterModelSwap() (MainComponent.cpp ~2364-2380), before rebuildGrid()/refresh().
     Both are message-thread-only and swapCompositionModel() is fully synchronous, so the
     30Hz timerCallback() (10Hz inspectorPanel_->refresh()) cannot interleave mid-swap —
     single message thread, no yield point inside the sequence.
   - Covers ALL paths: Open (loadComposition -> swapCompositionModel), New (kCompNew now
     routed through swapCompositionModel, replacing its old bare 4-line sequence), and a
     FAILED load (returns before ever calling swapCompositionModel — live model/inspectors
     untouched, nothing to null). Browser-row load and Cmd+O both funnel through
     loadComposition, same guarantee.
   - SECOND UAF class found by the builder, not in the packet's literal step list: the
     packet's §1 order runs `deckView_->setActiveColumn(-1)` BEFORE `rebuildGrid()`.
     DeckView::setActiveColumn -> refresh() -> `layerStrips_[row]->refresh()`
     (DeckView.cpp:203-249), and LayerStrip::refresh() does `if (!layer_) return;
     layerName_ = juce::String(layer_->name);` (LayerStrip.cpp:697-703) — `layer_` is a raw
     Layer* set by the OLD rebuildGrid() into the OLD (now-destroyed) Deck, and the null
     check only guards NULL, not DANGLING. I traced this myself: confirmed setActiveColumn
     really does call refresh() (DeckView.cpp:251-255), confirmed LayerStrip::refresh()
     dereferences layer_ unconditionally on non-null, and confirmed clearSelection()/
     selectLayer(-1) (the two other calls before rebuildGrid) are safe — they touch only
     coordinate ints and call setSelected(bool) on the widgets themselves (LayerStrip.h:39,
     ClipCell.cpp:379-386), never the model. The build reorders to clearSelection() ->
     selectLayer(-1) -> rebuildGrid() -> setActiveColumn(-1) (MainComponent.cpp ~2382-2399),
     documented in a block comment explaining exactly this. Verified rebuildGrid() seeds
     each cell's "active" highlight from `layer->activeClipColumn` (the NEW model), not from
     DeckView's own activeColumn_ member, so the reorder changes nothing else — it only
     moves the ONE unsafe call to after the point where it's safe. This is a genuine,
     correctly-diagnosed, correctly-fixed defect in the packet's own prescribed order, well
     documented in-code.

2. ORDER OF OPERATIONS — matches the architect's decision, not the recon's rejected one.
   swapCompositionModel() is byte-for-byte the packet's pseudocode: before-ids ->
   withDeckDetached(mutation) -> after-ids -> idsRetired() close -> undoManager_.clear() ->
   refreshUiAfterModelSwap(). Close-by-set-difference runs AFTER the swap (MainComponent.cpp
   ~2445-2451), not before — the recon's rejected ordering was NOT reverted to.

3. FAILURE POSTURE — verified exception-safe and side-effect-free on refusal.
   loadComposition stages into a private `incoming` throughout STAGE/VALIDATE/RE-MINT/OPEN
   NEW/NAME; only SWAP touches composition_. Composition::loadFromFile (Step 1, not
   re-reviewed) uses juce::JSON::parse, which returns var() on malformed JSON rather than
   throwing, and file.loadFileAsString() returns "" on a missing file — both are caught by
   early-return checks, no exception path exists. Composition's move-assignment is the
   implicit one over std::vector/std::string/juce::File/PODs (noexcept, cannot fail), so a
   half-swapped state is structurally unreachable, matching the packet's proof in §4.

4. ID RE-MINTING — compload::remintClipIds(incoming, s_nextClipId) runs BEFORE the OPEN NEW
   loop (MainComponent.cpp step 3 before step 4), against the correct TU-local file-static.
   No second minter introduced.

5. MEDIA RECONNECT — mirrors applyFileDrop's video block and applyMultiFileDrop's sequence
   block field-for-field (confirmed side by side against MainComponent.cpp:4098-4218).
   Missing video file: `!clip.mediaFile.existsAsFile()` skip, non-fatal. Missing/moved
   sequence files: ImageSequence::open() internally filters by existsAsFile() and fails
   closed (returns false, no crash) if all are gone (ImageSequence.cpp:17-38) — matches
   existing convention exactly, not a regression. Runs synchronously on the message thread
   (FileChooser launchAsync callback / browser-row mouseDown, both message-thread), same as
   every other file-drop path in this file. No double-open: fresh ids from step 3 never
   collide with a live map entry.

6. UNDO — undoManager_.clear() runs inside swapCompositionModel, so on every model swap:
   Open, New, and (once Step 3 lands) deck-append. Save/Save As correctly do NOT clear undo
   (no model swap occurs). DeckView's own stale-selection state is defensively pruned inside
   rebuildGrid() itself (existing code, unmodified) — no coordinate/pointer survives.

7. FX-PRESET SURFACE — savePresetButton_.onClick / loadPresetButton_.onClick (MainComponent
   .cpp:33-34) are untouched, still call savePreset()/loadPreset() directly. Only the 3 menu
   cases and the 2 keyPressed branches were rewired to the composition path. Grepped for all
   remaining savePreset()/loadPreset() call sites — only the two row-1 button callers remain.

8. Cmd+O / Cmd+S — both now call handleMenuCommand(AudioDNAMenuBar::kCompSave/kCompOpen)
   (MainComponent.cpp ~2727, ~2753), which route to saveComposition()/openComposition(). No
   other keyboard/menu path still calls savePreset()/loadPreset() under a composition-sounding
   label.

9. THE CLAMP — kMaxNumColumns (10000) is checked in validateDeck() BEFORE the `int cols =
   std::max(1, d.numColumns)` line and before any ensureColumns() resize
   (src/core/CompositionLoad.h), on the only call path into it (validateComposition, called
   once per deck from loadComposition's VALIDATE step). Confirmed genuinely red against
   b5a181c: `git show b5a181c:src/core/CompositionLoad.h` has no kMaxNumColumns and no bound
   check — that commit's validateDeck would set numColumns=50000 and resize every layer's
   clips vector, so the new test's `REQUIRE_FALSE(reason.empty())` and
   `REQUIRE(clips.size() == 3)` would both fail there.

10. FENCE — `git diff --name-only` (source) shows exactly the 5 expected files: src/
    MainComponent.{h,cpp}, src/ui/CompDecksBrowser.h, src/core/CompositionLoad.h, tests/
    test_composition.cpp. Also present: graphify-out/*.json + GRAPH_REPORT.md — these are
    Harmony's own auto-generated graph-cache artifacts (a post-commit-hook/launchd data
    pipeline per .harmony/gotchas.md and .harmony/inbox.md in that repo), not source, not
    hand-edited by the builder, and not part of this lane's behavior. Flagging as an
    informational note, not a fence violation. The untracked memory/ dir is the reviewing
    session's own, per the packet's instruction — ignored.

11. SCOPE — nothing else crept in. §7's explicit "not this lane" items (L1-FU, CompositorEngine
    GL release, kCompNew mutation widening, dirty-flag prompt, recent-files/autosave, TopBar
    sync, ApiServer marshaling, layer-id re-mint for hand-edited files, format version field,
    PresetManager/row-1 buttons, P23 UI) are all untouched — confirmed via fence check plus
    reading the full diff.

## Findings

No blocking issues.

Suggestion (non-blocking, cosmetic): the retained comment above `case C::kCompNew:`
("Sequential, not nested — the fence has already returned before either runs") describes the
OLD 4-statement sequence that swapCompositionModel() now replaces; still true in spirit but
slightly stale prose. Worth a light touch-up next time that block is edited — not worth a
separate commit now.

Worth feeding back to the packet/notebook (reusable principle, not a defect in this build):
the packet's own §1 step order (deckView_->setActiveColumn(-1) before rebuildGrid()) would
have shipped a real UAF; the general lesson is "never call a widget-container's own
refresh()-that-touches-children between destroying the model and rebuilding those children —
a container-level refresh can walk not-yet-replaced children's raw model pointers even when
the model-holding widgets one layer up (inspectors) are already nulled."

## What only a human at the machine can verify

1. Save a composition with >=2 decks, >=2 video clips and one image sequence; quit; relaunch;
   File > Open it; trigger the clips — confirm every cell shows its thumbnail and the video/
   sequence actually advances (proves OPEN NEW + re-minted ids connect at runtime, not just
   compile).
2. With the Clip/Layer inspector showing a clip's detail AND the deck grid visible (so both
   ClipInspector/LayerInspector AND LayerStrip/ClipCell widgets are live and pointing at real
   clips), do File > Open to a DIFFERENT composition — confirm no crash and the grid/inspector
   both update to the new deck immediately. This is the concrete repro shape for both UAF
   classes above; a source review cannot fully substitute for watching it not crash under
   real GL/timer timing.
3. Cmd+S / Cmd+O specifically (not the menu) — confirm they act on the composition (file
   label/browser update), and the FX-preset row-1 Save/Load buttons still work independently.
4. Save vs Save As: Save on an unsaved comp prompts Save As; after Save As, plain Save is
   silent (check file mtime, no dialog).
5. Refusal: Open an FX-preset .json from PresetManager's presets dir — one alert, nothing
   changes, the running deck keeps playing.
6. Global FX rows: comp A (3 global effects) vs comp B (0) — Composition tab's effect list
   updates correctly after each Open (RECON TRAP #2 regression check).
7. Old comp gone: an active MilkDrop/procedural source, then Open a comp with none — output/
   preview go dark rather than bleeding the old source through.
8. Browser row click on a saved composition — same correctness as File > Open.
9. Expected/known stale (recorded, not a regression): TopBar fade/quantize/multiplier
   widgets show pre-load values until touched.
10. Craft/hand-edit a deck file with "numColumns": 50000 and try to load it — confirm a clean
    refusal alert rather than a multi-second UI freeze from a huge resize attempt.

METADATA: reviewer=rev-L3S2, builder_packet=L3-composition-persistence.md STEP 2,
date=2026-09-05, base_commit=b5a181c (L3 Step 1), diff=working-tree (uncommitted)
