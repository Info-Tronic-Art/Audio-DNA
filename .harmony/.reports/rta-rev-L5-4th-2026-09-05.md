# Reviewer Verdict — rta-rev-L5-4th-2026-09-05
STATUS: DONE
VERDICT: PASS_WITH_FINDINGS

FILES: src/core/DeckCommands.h, tests/test_undo_commands.cpp (RemoveDeckCmd slice only —
src/model/Layer.h, src/model/Deck.h, src/MainComponent.cpp are prior-round content, not re-reviewed)

## Answers

1. RemoveDeckCmd::undo() coverage / redo correctness:
   undo() correctly covered — DeckCommands.h:898-899, unconditional cancelPendingTriggers()
   on whatever deck is currently active, run BEFORE insert(). Verified live: temporarily
   neutralized this call (`if (false && ...)`), rebuilt, both new tests failed
   (5 == -1 mismatch), restored — confirms it's load-bearing, not vacuous.
   REDO is NOT symmetric. undo()'s cancellation is fire-and-forget: no member stores what
   was cancelled, unlike SwitchDeckCmd (cancelledOnLeave_) / AddDeckCmd (cancelledOnAdd_),
   which both capture-and-restore-and-reapply. Proved live with a temporary probe test
   (appended, run, reverted — tests/test_undo_commands.cpp restored byte-identical after,
   diff-verified): perform(RemoveDeckCmd) -> arm trigger on clamped-to deck -> undo()
   (cancels it, correct) -> redo() -> trigger stays -1, never restored to its pre-undo
   value. Unreachable via the app's OWN undo-tracked trigger commands (any TriggerClipCmd
   sits strictly above RemoveDeckCmd on the linear undo stack and is undone/redone in its
   own right first, so by the time RemoveDeckCmd.undo() runs nothing legitimate is armed
   there). BUT Autopilot.cpp:288,310,372 call `layer.triggerClip(...)` directly — a
   non-undo-tracked trigger source — so an Autopilot-armed trigger sitting on the
   clamped-to deck at the moment of undo can be silently DROPPED (not mis-fired) by a
   subsequent redo. Lower severity than the bug class this lane targets (lost trigger, not
   a late fire on the wrong deck) but it's a real asymmetry against this file's own stated
   convention ("blindly re-impose the recorded cancellation ... not a freshly recomputed
   one").

2. "Numerically stays the same" — builder is right, verified from source. RemoveDeckCmd's
   guard at DeckCommands.h:898 is genuinely unconditional (no index-equality check), so it
   doesn't share the blind spot it names. Checked all three other sites for the same shape:
   - handleDeckSwitch (MainComponent.cpp:4447, `if (deckIndex != composition_.activeDeckIndex)`)
     compares the caller-supplied TARGET index to current active — safe, deckIndex is a
     direct target, not a slot number subject to insert/erase shifting.
   - AddDeckCmd (DeckCommands.h ~716/738/765): never compares indices — unconditionally
     targets priorActiveIndex_, bounds-checked only; append-only insert never shifts
     anything below the append point (comment's own invariant holds).
   - SwitchDeckCmd::apply(): always targets the fixed `before_` deck by construction, no
     index-equality check anywhere.
   None use an "only if index changed" guard, so none share RemoveDeckCmd's blind spot.

3. Both new tests are real. Verified by temporarily neutralizing the undo() fix
   (single-line edit, DeckCommands.h:898), rebuilding, and rerunning: both fail
   (`5 == -1`) against pre-fix code; reverted after (diff-confirmed byte-identical).
   Both drive the actual production RemoveDeckCmd via mgr.perform()/mgr.undo()
   (UndoManager::perform/undo call cmd->execute()/cmd->undo() directly — not a stub,
   confirmed by reading UndoManager.cpp), not a reimplementation.

4. No regressions found. Full test_undo_commands suite: 69/69 test cases, 453/453
   assertions green after a clean rebuild. The 8 [quantize]-tagged cases (the 3
   previously-reviewed sites + all 5 new cases) are all green.

5. Uncovered: the Q1 redo-asymmetry (RemoveDeckCmd's cancellation isn't captured/restored
   on redo) — confirmed live via the probe above, not hypothetical. Swept: read
   RemoveDeckCmd's full execute()/undo(), diffed its shape against SwitchDeckCmd's/
   AddDeckCmd's capture-restore-reapply pattern, grepped every triggerClip/triggerColumn
   call site in src/ to find the non-command-tracked path that makes it reachable
   (Autopilot.cpp). Did not find a second gap beyond this one.

## Recommendation
Not blocking given severity (dropped queued trigger, not a wrong-deck fire) and narrow
scope, but worth a one-line fix or an explicit "known gap" comment in a follow-up: either
capture cancelPendingTriggers()'s return in a member and restore/reapply it symmetrically
in execute() (matching SwitchDeckCmd/AddDeckCmd), or document why it's accepted as-is.

METADATA: reviewer=rev-L5-fix, builder_packet=rta-rev-L5-4th-2026-09-05, date=2026-09-05
