---
subject-project: RealTimeAudio
---
# s158 N12-rta — TEAMMATE PACKET: normalize Phases 1-2 (read-only audit + gap report) for `/Users/boriskarpman/projects/RealTimeAudio` (Boris: "update these secondaries" — same treatment as the T-shirt, s158)
DISPATCH: teammate

TASK: run `.claude/skills/normalize/SKILL.md` (in /Users/boriskarpman/Harmony_Main) **Phase 1 (secrets scan) and Phase 2 (discovery + gap analysis) ONLY** against the project `/Users/boriskarpman/projects/RealTimeAudio` (QUOTE the path everywhere). Read the skill + `references/discovery-checklist.md` + `references/context-and-gotchas.md` first, then the project .harmony/HANDOFF.md and .harmony/inbox.md (any `status: SENT` record: quote its id + raw line; do NOT flip it — the secondary does that at boot). Known state at dispatch: registered (config/repos.yml:50); .harmony/ has 45 entries; 268 dirty files; normalize-check reads NEEDS FIXES (template validation failed) — run bash tests/validate-features.sh <path> --framework <type> from Harmony_Main and quote each FAIL item; Tracked issue: HEAVY drift 116 files/121 commits since the last normalize. Produce the Phase-2 report in the skill format (gaps REQUIRED / RECOMMENDED / NICE_TO_HAVE, MCP recommendations for the detected stack, the Phase 2.x structure classification) PLUS: (a) "WHAT THE EXISTING .harmony/ ALREADY HAS" — map every existing .harmony/ file/dir to its current-template home (folds into X / stays / project-local machinery to keep / stale leftover) so Phase 3 preserves rather than overwrites; (b) "DIRTY TREE" — `git status --short | wc -l`, the dirty set grouped by kind (tracked-modified / untracked / backups such as *.pre-s144-migration / stray archives) with a one-line commit-or-clean recommendation per group — NEVER stage, commit, clean or delete anything; (c) if normalize-check reads "Current SHA" (normalized once, drifted): run `bash /Users/boriskarpman/Harmony_Main/scripts/normalize-check.sh --classify "/Users/boriskarpman/projects/RealTimeAudio"` if that flag exists and report FEATURE_AFFECTING vs not. Phase 1 output: the secrets-scan verdict (paths only, never the secret). Do NOT run Phase 2.5 or Phase 3. DEPARTMENT: system. SKILL: normalize. PROJECT: RealTimeAudio. MODEL: sonnet/high.
SUCCESS_CRITERIA: `/Users/boriskarpman/projects/RealTimeAudio/.harmony/.reports/N12-rta.report.md` exists (skeleton FIRST with `STATUS: PENDING`, full content LAST), ends with a `## BORIS PICKS` checklist (`[ ]` per gap, REQUIRED first) and the last line `STATUS: DONE`; `bash /Users/boriskarpman/Harmony_Main/scripts/normalize-check.sh "/Users/boriskarpman/projects/RealTimeAudio"` output pasted verbatim (the baseline); `git -C "/Users/boriskarpman/projects/RealTimeAudio" status --short` shows ONLY your report as new beyond the pre-existing dirty set.

## FENCE — allow-list
`/Users/boriskarpman/projects/RealTimeAudio/.harmony/.reports/N12-rta.report.md` (the ONLY write; create .harmony/.reports/ if absent). Read anything in that project and in /Users/boriskarpman/Harmony_Main. Runners permitted: `bash scripts/normalize-check.sh` (read-only), the skill Phase-1 secrets-scan commands, `git status/log/diff --stat` (read-only), presence checks only (no build, no test run).
FORBIDDEN: any write to Harmony_Main (memory/, config/, .claude/) or ~/Vaults/Company_HQ/SHARED_STATE.md; any other write in the project; git add/commit/checkout/stash/reset/clean in either repo; flipping inbox records; creating teams or agents. Bash shell is zsh: explicit `bash` for scripts, quote every path.
REPORT_FILE: /Users/boriskarpman/projects/RealTimeAudio/.harmony/.reports/N12-rta.report.md
