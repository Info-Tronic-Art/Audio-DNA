# FLAGGED Triage — 2026-07-17 (Boris-ratified, session ledger)

Source: APP-INVENTORY.md §8 (norm v2, HEAD 9139dd4). All 34 rows dispositioned.
Plan approved by Boris 2026-07-17. Commits LOCAL ONLY — no push (standing CI rule).

## Boris decisions (ratified via AskUserQuestion)
1. Video I/O: wire **Syphon output**; CUT Syphon input, Spout, NDI in/out.
2. ISF import: **IMPLEMENT real compile support** (Wave 2).
3. Undo/redo: **IMPLEMENT real undo** — v1 scoped to structural edits (no slider moves).
4. Session Recorder: **IMPLEMENT fully** (7 event types + playback) (Wave 2).

## Wave 0 — Purge + XS fixes + UI honesty — status: DISPATCHED
- DELETE: shaders/ disk files · UniformBridge/applyDemoMappings · MappingSuggester ·
  ChainedSignal · GenreSmoothing · OneEuroFilter · ProgrammingMode · source_fluid_display ·
  orphaned tuning setters · ISFShaderLoader::registerISFEffect stub · SyphonInput ·
  SpoutOutput · NdiOutput/NdiInput.
- KEEP (later waves): SyphonOutput (W1-A), UndoManager/Command scaffold (W2 replaces),
  SessionRecorder (W2), ISF import menu path (W2 implements).
- XS FIX: FeatureSnapshot::clear() genre/energy defaults · +6 sources → SourcesBrowser ·
  /api/set_bpm wired to manual-BPM path.
- UI: remove ~35 no-op menu items · remove 4 empty Prefs tabs + inert combos.
- Same-wave: APP-INVENTORY §8 rows + §2 counts updated by builder.

## Wave 1 — after Wave 0 lands — status: PENDING
- A: Syphon output wiring (SyphonOutput.mm, MainComponent.cpp:390, render loop).
- C: Lossy persistence round-trip (Clip/Layer/Composition toVar/fromVar + back-compat
  load + round-trip unit tests). Runs parallel with A (disjoint files).
- B: OSC — startListening() + wire 6 missing callbacks (after A/B MainComponent settles).
- D: TopBar Play/Pause/Stop wire · waveform torn-read fix · runtime SR guard (warn ≠48k).
- Verify: full-tier each — independent Reviewer on source + Harmony runs behavioral gate
  (build + ctest + REST/OSC probes; Syphon publish needs eyes-on Syphon client — flag Boris).

## Wave 2 — specs this session, build next sessions (drain long-task rule)
- ISF real import — spec LANDED → .harmony/specs/isf-import-spec.md (~6-9 builder-days, 8 steps)
- Undo v1 (structural only) — spec LANDED → .harmony/specs/undo-v1-spec.md (value-copy
  commands on existing scaffold; NO dependency on Wave-1 persistence fix)
- Session Recorder full — spec LANDED → .harmony/specs/session-recorder-spec.md (407 lines,
  verified on disk; wall-clock replay, v2 schema, state machine). **SEQUENCE AFTER WAVE 1**
  — Wave-1 D (TopBar transport) + B (OSC rewire) create/change its capture sites.
- Cross-spec invariant (recorder↔undo): performance paths (handleClipTrigger,
  handleBindingAction, applySessionEvent, capture sites) NEVER call undoManager_.perform();
  replayed events are NOT undoable. Verified true today; holds by design in both specs.
- Wave-2 build order (derived): Undo v1 → Session Recorder (after Wave 1) · ISF independent,
  any time after Wave 0 (its Step-1 shared_mutex fixes the EffectLibrary race).

## NEW FINDINGS from plan agents (not in original 34 FLAGGED rows)
1. **kLayerClearClips clears the ENTIRE DECK** — body identical to kDeckClearClips
   (MainComponent.cpp:2940-2952 vs :2906-2918). Almost certainly a bug. → Boris decision;
   candidate Wave 1-D fix. (plan-undo, verified in source)
2. **EffectLibrary data race (pre-existing)** — message thread mutates defs_
   (MainComponent.cpp:2443) while GL thread reads every frame (CompositorEngine.cpp:245,258).
   Fix scheduled inside ISF Step 1 (shared_mutex). (plan-isf, verified)
3. **ISF converter output is discarded** — converted GLSL local at MainComponent.cpp:2426
   never used; also v_uv vs v_texCoord guaranteed link failure. Deepens the §8 PHANTOM row;
   fixed by ISF spec Steps 0-1. (plan-isf, verified)
4. **Coordination note:** Undo build step 1 touches MenuBarModel.cpp — same file Wave 0
   menu-honesty edits touch. Sequence undo AFTER Wave 0 lands (already the plan).

## Deferred (documented, untouched)
Dual mapping-engine write-order (design needed) · OutputWindow dup shader table ·
Global-only MacroBank · TimingWindow/EffectsRack/MappingEditor/Spectrum hidden-surface
decisions (interact with mapping design) · Ableton Link (OFF) · shader hot-reload (inert
by embedded-shaders doctrine).

## Gate results
- **Wave 1 behavioral gate: PASS** (2026-07-17). Build clean; ctest 114/114; app boots via
  `open`; OSC proven live end-to-end over real UDP:8000 — /audiodna/bpm 122 → /api/bpm 122.0;
  /audiodna/master 0.55 → composition masterOpacity 0.55; /audiodna/layer/0/bypass →
  bypassed:true. (Probe note: /api/status masterLevel reads renderer_.getMasterLevel(), NOT
  composition masterOpacity — first master probe false-alarmed; verify OSC master via
  /api/composition.) Persistence proven by the 4 in-suite round-trip tests. Syphon =
  compile-level verification only (framework absent; Boris setup step stands).
- Wave 1 commits: 71f2387 (A) / 6484aba (C) / 9a6d7f0 (B) / fb271e3 (D), local only.
  B choice: OSC port 8000 (TouchOSC default; alt 7000 Resolume-style if Boris prefers).
  D choices: transport = active-deck all-layers (Play fwd / Pause hold / Stop rewind-to-in);
  waveform fix pivoted double-buffer→SEQLOCK because the builder's own stress test caught
  the double-buffer lapping tear. Clear-Clips no-ops when no layer selected (-1).
- **Wave 1 Reviewer: PASS_WITH_FINDINGS — Wave 1 CLOSED** (gate PASS + review PASS).
  Deep-verified: persistence key-by-key (zero typos, bounds-safe arrays, no runtime field
  serialized); OSC = MessageLoopCallback per JUCE header → all 11 callbacks on message
  thread, no new race, hostile-input safe; seqlock = textbook idiom, writer wait-free,
  correct fence pairing; Syphon GL-state restore + no-op build path + lifecycle clean;
  Clear-Clips selection path traced genuine.
  FINDING (MEDIUM, hygiene): APP-INVENTORY.md was force-added inside the Syphon commit
  (disclosed by builder, undisclosed in commit msg). DISPOSITION: accept — .harmony/*.md
  siblings are tracked, file SHOULD be tracked; history rewrite not worth it. Process rule
  for future packets: force-adds get their own commit + explicit mention.
  FINDING (LOW, PRE-EXISTING): Clip::playing is a plain bool written from message thread
  / read+written on GL thread — same unsynchronized model-race family as dual-mapping-
  engines + undo-spec risk #1. → added to Deferred as one family: "model thread-safety
  design (atomic playing / double-buffered model handoff / engine write-order)".
- Doc-sync builder: running (consolidation + stale-prose fix). EOS after it lands.
- **Wave 0 behavioral gate: PASS** (2026-07-17). Build clean; ctest 109/109 (113−5 OneEuroFilter
  tests removed with class, +1 clear() test); app boots to full v2 UI via `open` (direct exec
  hangs — see gotchas); POST set_bpm 137 → GET bpm 137.0 + TopBar shows "137 LOCKED";
  all 6 new sources in /api/sources. Builder status DONE_WITH_CONCERNS → concerns resolved:
  #1 set_bpm proven live; #2 projectm needs libprojectM (pre-existing, accepted); #3 Show-Tooltips
  onTooltipToggled never assigned in MainComponent → folded into Wave 1-D; #4 dead menu enums
  kept deliberately (range-sentinel math) — Reviewer to confirm; #6 graphify churn = background
  hook, not builder's.
  RESIDUAL (Boris eyeball, 10s): menus, Prefs (now 3 tabs), Sources rows render check.
- Wave 0 commits: 368d621 / 45ae7e8 / 0dd0388 (local only).
- **Wave 0 Reviewer: PASS_WITH_FINDINGS (no blockers)** — all 13 deletions grep-clean
  tree-wide; set_bpm wiring functionally identical to TopBar path + double-guarded;
  6 source rows match registry exactly incl. colors; all 37 removed menu items confirmed
  no-ops; enum-sentinel math holds (kViewProgrammingMode enum delete safe — next block
  resets =1850); Prefs rewrite dangling-ref-free; test edits correctly scoped.
  Info notes: (1) stale prose in CLAUDE.md / .harmony/FEATURES.md / docs/design archives
  still describes deleted symbols → Harmony doc-sync before EOS; (2) Show-Tooltips plumbing
  was a dead end PRE-Wave-0 (tooltipsEnabled_ never set) → confirms Wave 1-D tooltip wire.
- **Wave 0 = CLOSED: gate PASS + review PASS.**
- Wave 1 A (Syphon) + C (persistence) dispatched in parallel; B (OSC) + D (small batch +
  kLayerClearClips fix + Show-Tooltips wire) follow after A lands.
- **Wave 1-A LANDED** (71f2387, DONE_WITH_CONCERNS): init at GL-context creation, per-frame
  letterboxed blit+publish, Output→"Syphon Output" toggle (default OFF, no settings store
  exists), atomic-flag threading, FBO restored for capture path. ctest 109/109.
  **MATERIAL:** Syphon compiles to NO-OP unless -DAUDIODNA_BUILD_SYPHON=ON AND
  Syphon.framework installed — framework NOT present on this machine (checked) → real
  publish verification impossible here. → BORIS SETUP STEP: install Syphon.framework,
  rebuild with the flag, eyeball in a Syphon client; then consider flag default ON.
  Reviewer on A's source: queued into the combined Wave-1 review after B/D land.
  Builder-reported gotcha to sync into gotchas.md at doc-sync (gotchas.md busy with C):
  any post-render GPU step must restore GL_FRAMEBUFFER=defaultFBO — processPendingCapture's
  glReadPixels depends on it.
- Wave 1-B+D dispatched as ONE combined builder (parallel would contend on
  MainComponent.cpp + build dir).
- **Wave 1-C LANDED** (6484aba, verified 4 files exactly): full round-trip for every dropped
  Clip/Layer/Composition field + EffectSlot.dryWet at all 3 sites; pure-additive back-compat
  (hasProperty-guarded); +4 round-trip/back-compat tests → 113/113 (builder-run; Harmony
  re-runs at final gate). Nuance: real Layer pair was transitionMode+transitionBlendMode
  (blendMode was already serialized). Excluded-as-runtime list recorded in builder report —
  OPEN QUESTION for Boris (minor): should MilkDrop playlist POSITION persist? (currently
  runtime-only.) Doc edits (§5 LOSSY→COMPLETE, §2 count 113, gotchas FIXED note) on disk
  UNCOMMITTED → Harmony consolidates into doc-sync commit after B+D.
