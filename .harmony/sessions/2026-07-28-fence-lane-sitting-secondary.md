# Session log — 2026-07-28 — Manual e2e sitting (partial) + GL-fence family lane (Harmony secondary, slim boot, Boris-present)

| field | value |
|---|---|
| role | secondary (boot ~/Harmony slim; work repo ~/projects/RealTimeAudio) |
| directive | Boris chose "Sitting, then cert" (AskUserQuestion); mid-sitting Column→New CRASH pivoted the session; Boris ratified "Family fence fix + cert" (AskUserQuestion); closed with app gate parked on the unanswered TCC Allow click |
| method | sitting driven from .harmony/undo-v1-manual-e2e.md (Harmony feeds batches, ticks on disk); crash triage = parse .ips faulting stack FIRST, then read-only Explore scout with the stack; fence lane = Builder → independent Reviewer → Harmony gate (receiver disk-verify, build, ctest own-run) per round; 3 build rounds + 1 fix round + 2 micro-edits; agents: drop-scout, crash-scout (Explore), fence-f1 (2 packets + fix + micro-edit, retired), fence-f3 (1 packet + micro-edit, retired), fence-f1-review (2 full + 3 targeted + addendum, retired) |
| shipped commits | **8bd09ba fix(render): GL-fence family** — fences EVERY swept message-thread model mutation against the unlocked GL render thread: 12 live handler sites (columns/clears/clip-clear/drop-growth), 6 fenced commands (SetColumnCountCmd/RemoveColumnCmd/ClearLayerClipsCmd/EffectStackCmd/SetClipCmd/SwapClipsCmd via DeckFenceHook), kCompNew initDefault, handleFileDrop+handleMultiFileDrop, effects vectors (clip proven GL-read :242, layer copy-assign :752; global fenced uniformly), kClipReplaceContent; withDeckDetached RAII exception-safe + fenceActive_ reentrancy jassert; CMake AUDIODNA_CODESIGN_IDENTITY post-build codesign w/ ad-hoc fallback. 16 files +777/−246. LOCAL (no-push) |
| tests | 170/170 baseline (inherited, corroborated) → 173 (r1) → 174 (r2) → 176/176 (r3); every count re-run by Harmony own-run (×4 gates + final re-run); 6 new fence-invocation-count tests |
| crashes | #1 Column→New SIGSEGV GL thread (.ips 182825): scout diagnosis DISASSEMBLY-verified (binary UUID matched) — PRE-EXISTING UAF, clips.resize under unlocked renderOpenGL holding interior Clip*; crash registers held rebuildGrid geometry doubles; spec's "status-quo risk profile" FALSIFIED → LAW in notebook. FIXED (8bd09ba). #2 SignalBar-arrow → MilkDropBrowser::getCuratedPresets null-deref (.ips 190701) — separate, message-thread, empty-preset-state; QUEUED (bundle w/ default-preset-dir ask), Boris nod pending |
| review arc | r1 REQUEST_CHANGES (2 MAJOR: withDeckDetached not exception-safe — NEW-code catch; kCompNew unfenced — sweep catch) → fix round CONFIRMED-CLEAN; r2 FINDINGS (area-5 ruling: effects vectors blocker-class; builder concern ruled REAL+WIDER: occupied-cell replay + 2 never-fenced live drop paths) → r3 prescription → final confirm CONFIRMED-CLEAN. Addendum: CompDecksBrowser load/save-comp callbacks UNWIRED (inert landmine) → FUTURE-FENCE comment + inventory row corrected to "partial" |
| sitting results | 10 PASS (preconditions ×2, drop-undo, drag-move-far, menus/prefs eyeball, multi-video-edge, multi-SOURCE, multi-FX-far, swap-undo, 3-tab prefs); FAIL: Column→New crash; discovered: mixed-drop (2 vid+png) silently discards images (ClipCell external filesDropped video-first early-returns — internal path handles mixed correctly; scouted, queued); "Edit menu" = none — undo lives in Composition menu; MilkDrop browser empty (dir unset + libprojectM absent); "Audio-DNA" duplicate menu cosmetic. PASSED drop tests are RACE-LUCKY (rode exposed paths) → re-run post-fence |
| app gate | PARKED: fenced build relaunched 20:09, TCC dialog confirmed on screen via screencapture; 10-min + 30-min watchers expired unanswered (Boris away); app LEFT RUNNING w/ dialog (07-25 precedent). Cert NEVER created (keychain 0 identities at 3 checks) → build ad-hoc; after cert: cmake RECONFIGURE required (identity resolved at configure time) then rebuild |
| decisions | RATIFIED (Boris): sitting-then-cert order; family fence fix + cert bundle (ASan pre-confirm skipped — disassembly-grade evidence). OPEN queue grew: crash-#2+preset-dir bundle · mixed-drop fix · Cmd+X cut-clear (idea-ledger) · MilkDrop default presets (idea-ledger) · browser load/save wiring vs defer to recorder wave · refreshAfterUndoRedo · zero-layer Deck-New · inspector-clear UX · Syphon · B8 |
| receiver-verify | every builder report diff-scoped vs disk pre-acceptance (16-file claim verified exactly); reviewer claims spot-checked (typedef single-def, exemption-block deletion, RAII guards) before dispatching dependents; touched-repos.sh returned 7 machine-wide-dirty candidates at close → work repo disambiguated by THIS session's commit evidence (8bd09ba), not guessed |
| carry-forwards | lane B repo-local: HANDOFF.md (new birth prompt: Allow-click → app gate → cert → resumed sitting) · undo-v1-ledger.md (+11 entries: sitting, crashes, ratifications, 3 review rounds, commit, parked gate) · undo-v1-manual-e2e.md (ticks, findings, expanded-then-dissolving HOLD, §7 narrowed) · notebook.md (LAW + scope riders + 2 bug entries) · APP-INVENTORY.md (CompDecks row → partial) · idea-ledger.md (2 canonical records) · this log. harmony2 writes: ZERO (clean secondary) |
| works/doesn't (delta) | NOW WORKS (code-proven, review-confirmed): the entire message-thread-mutation-vs-GL-read crash class is fenced, incl. undo/redo replay; exception-safe fence internals; stable-signing plumbing ready (awaiting cert). STILL PENDING: app-level proof of the fenced build (one Allow click away); the whole remaining manual checklist; visual-design track unchanged (dormant at v10 row partB, no ratified winner — next-lane fork: Recorder vs ISF vs visual-design resume) |
