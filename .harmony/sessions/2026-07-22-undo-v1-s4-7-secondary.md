# Session log — 2026-07-22 — Undo v1 steps 4-7 (Harmony secondary, slim boot, autonomous drain)

| field | value |
|---|---|
| role | secondary (boot ~/Harmony slim; work repo ~/projects/RealTimeAudio) |
| directive | Handoff START-HERE (no live Boris all session): Undo v1 steps 4+ per spec §6, #1 Committed MUST; drain off-ramp ~40% |
| method | per-step lane unchanged: Builder → independent Reviewer on source → Harmony behavioral gate (receiver disk-verify, build, ctest own-run, residue grep, app `open`+health) → local commit; warm-builder R3 fix loops; reviewer rotation at ~2-packet load (undo-s4-review → undo-s6-review → undo-s7-review); lane state externalized to undo-v1-ledger.md before every dispatch |
| shipped commits | 6d2def4 step-4 composites (SetColumnCountCmd/RemoveColumnCmd/ClearLayerClipsCmd; column-growth gap CLOSED; onSourceDropped was fully un-undoable — now wrapped) · 7f87094 step-5 layer ops (command-owns-the-mutation pattern est.; DeckFenceHook; LayerInspector re-point) · 316a2bf step-6 deck ops (latent renderer re-point bug fixed under spec mandate; entry-point-only switch wrap vs 4 non-user call sites) · d90e953 step-7 effect stacks (EffectScope 3-scope performEdit; remedy-(b) comment-truth). ALL LOCAL (no-push rule) |
| tests | 130/130 baseline verified → 158/158 at close (+8 [column/clear], +7 [layer], +8 [deck], +5 [effect]); every count re-run by Harmony, never accepted from builder logs |
| gates | s4: CLEAN + jassert NIT fix + targeted re-review; gate PASS ×2 (app health 19s post-stall). s5: CLEAN 10/10, trivial NIT → step 9; code gates PASS; APP GATE BLOCKED-ENV (coreaudiod wedge, 4 attempts, verified change-independent — 0 audio refs in diff, stall predates code). s6: CLEAN + 2 targeted re-reviews (jassert + test-invariant one-char fix); code gates ×3. s7: FINDINGS (1 MAJOR: false comment guarantee) → remedy (b) → CONFIRMED-CLEAN + UNBLOCK (reviewer's independent call); code gates ×2 |
| bugs found+fixed | Latent HEAD: kDeckNew/kDeckRemove never re-pointed renderer after decks-vector mutation (dangling activeDeck_) — fixed via fence re-resolve, spec §6 row 6 mandate, reviewer-verified nothing smuggled |
| pre-existing surfaced NOT fixed | deck-tab highlight never updates via rebuildGrid (undo makes visible); expanded-FX-row collapse on ANY undo (unconditional refreshAfterUndoRedo); raw Deck-New = zero layers, id=0 collisions; SyncScope::DeckStructure + LayerStrip onFoldToggle dead code; dead 2-arg reinspect path scope-None trap |
| decisions | fix-vs-defer split by class: invariant-defense jasserts = fix-now (s4/s6 parity); pre-existing cosmetics = checklist + Boris-ratified follow-up (refreshAfterUndoRedo pointer/scope-aware skip); remedy (b) over (a) for s7 MAJOR (shared-path change not smuggled at 35% gauge; reviewer invited to overrule — chose UNBLOCK citing s6 precedent) · budget rider pre-declared at s7 dispatch (beyond-NIT → HOLD) · steps 8-9 DEFERRED at ~38% (LONG-TASK DEFERRAL) |
| receiver-verify | all 4 builder reports diff-scoped vs disk pre-acceptance; ctest logs cross-checked but never trusted (own runs ×9); step-5 673-line graphify churn excluded from scope check; work-repo root resolved by today-commit SHA match (touched-repos 7 dirty candidates, prior-session precedent) |
| gate diagnostics | coreaudiod wedge WORSENS with pkill -9 cycles: s4 gate recovered attempt-2; s5 gate 4 attempts all stalled (sample ×2: CoreAudioInternal::start mutex), 90s settle no help; s6/s7 single-recheck policy (no thrashing). Needs Boris: sudo killall coreaudiod or reboot. New gotcha appended |
| inventory currency | same-wave rule honored: undo row → steps 1-7 coverage, shortcut line broadened, files list + known-cosmetics noted (this close). Step 2b CLEAN |
| carry-forwards | lane B: HANDOFF.md (birth prompt = step 8 START HERE + env remedy FIRST) + undo-v1-ledger.md (Boris decision queue ×4, step-9 fold-ins, manual e2e additions ×3 steps) + gotchas.md; zero harmony2 writes (clean secondary); zero Boris turns → no idea-ledger records owed |
| works/doesn't (delta) | NOW WORKS (headless-proven; live UI pending env unblock): undo/redo for composites + column ops (growth gap closed), layer add/remove/move (GL-fenced)/clear/bypass/solo/fold, deck add/remove (fenced, renderer re-point)/tab-switch, effect add/remove/bypass across clip/layer/global scopes. STILL PENDING: trigger cmds + merge (step 8), test/e2e pass + cleanups (step 9), ALL app-level manual checks (blocked on coreaudiod env) |
